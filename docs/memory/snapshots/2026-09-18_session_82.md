# MEMORY SNAPSHOT — Phiên Làm Việc #82
# Ngày: 2026-09-18 | Nội dung: Triển Khai Phân Hệ Xuất Văn Bản DOCX/PDF Chuẩn NĐ 30 & Nạp Toàn Văn Nghị Định 30 Vào Database Mặc Định

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-18 11:25 (UTC+7)
- Giai đoạn vừa hoàn thành:
  1. Xây dựng phân hệ tạo và kết xuất văn bản hành chính chuẩn Nghị định 30/2020/NĐ-CP của Trường ĐH Quy Nhơn (`docxtpl` + Gotenberg 8 PDF engine + REST API download artifacts).
  2. Cấu hình Prompt 3 tầng tự nhiên cho Trợ lý Soạn thảo Văn bản (`ast_drafting`): Tư vấn $\rightarrow$ Chủ động gợi ý xuất file ở cuối câu trả lời $\rightarrow$ Kích hoạt công cụ sinh file khi người dùng đồng ý.
  3. Bóc tách toàn văn 11 trang Nghị định 30/2020/NĐ-CP thành 6 Chunks chuẩn Chương/Điều và 7 Facts số hóa, nạp vĩnh viễn vào CSDL PostgreSQL (`col_drafting`) và Qdrant Vector Index dưới dạng Seed Data mặc định của nền tảng.
  4. Nâng cấp Frontend hiển thị thẻ tải file `.docx` và `.pdf` riêng biệt có dung lượng, màu sắc nhận diện và liên kết tải về trực tiếp.
- Test Suite: 44/44 pytest passed (100%), ruff 0 error, biome 0 error, tsc 0 error, vite build thành công (10.35s), mojibake audit 231/231 sạch 100%.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/templates/documents/` | NEW | 3 file template Word `.docx` chuẩn thể thức NĐ 30 của Trường ĐH Quy Nhơn (`mau_to_trinh_nd30.docx`, `mau_thong_bao_nd30.docx`, `mau_quyet_dinh_nd30.docx`). |
| `backend/app/modules/tools/document_generator.py` | NEW | Module sinh văn bản docxtpl + chuyển đổi Gotenberg 8 PDF + lưu trữ vào `storage_service` (`/platform/v1alpha1/tools/artifacts/{filename}`). |
| `backend/app/modules/knowledge/seed_data_nd30.py` | NEW | Bộ dữ liệu seed toàn văn 11 trang NĐ 30/2020/NĐ-CP (6 Chunks, 7 Facts số hóa, metadata thể thức). |
| `backend/app/modules/knowledge/seeder.py` | NEW | Hàm `seed_default_knowledge` nạp tự động tài liệu, chunks và facts NĐ 30 vào `col_drafting` và index vào Qdrant. |
| `backend/scripts/seed_knowledge.py` | NEW | Script độc lập nạp dữ liệu tri thức NĐ 30 vào CSDL PostgreSQL và Qdrant. |
| `backend/app/modules/workflows/nodes/artifact_export_node.py` | NEW | Handler cho node `artifact.export` kết xuất tài liệu docx/pdf từ nội dung hội thoại. |
| `backend/tests/test_document_generator.py` | NEW | 10 unit tests kiểm thử trọn vẹn render docx, Gotenberg fallback, export package, endpoint tải artifact. |
| `backend/app/modules/tools/router.py` | MODIFY | Thêm endpoint `GET /artifacts/{filename}` phục vụ tải về file văn bản và `POST /export/document`. |
| `backend/app/modules/tools/builtin/document_exporter.py` | MODIFY | Cập nhật kết nối `DocumentGeneratorService`, trả về `artifacts`, `docx_url`, `pdf_url`. |
| `backend/app/modules/assistants/seeder.py` | MODIFY | Cập nhật `ast_drafting` với system prompt 3 tầng và công cụ `document.docx_export`, `export_administrative_document`. |
| `backend/app/modules/assistants/schemas.py` & `service.py` | MODIFY | Bổ sung trường `artifacts` trong `AssistantChatResponse`, truyền `is_approved=True` và `format=docx,pdf` khi gọi workflow từ chat. |
| `backend/app/modules/workflows/nodes/output_chat_node.py` | MODIFY | Bảo toàn `artifacts` từ `context.node_data` chuyển vào `context.outputs`. |
| `backend/app/modules/workflows/engine.py` | MODIFY | Loại bỏ `artifact.export` khỏi `_TERMINAL_NODE_TYPES` để luồng DAG tiếp tục chạy sang node output. |
| `backend/app/modules/rag/vector_indexer.py` | MODIFY | Hỗ trợ tương thích cả `query_points` (qdrant-client hiện đại) và `search` (cũ). |
| `backend/app/main.py` | MODIFY | Hook `seed_default_knowledge` vào lifespan startup của FastAPI backend. |
| `configs/workflows/drafting-assistant.v1alpha1.json` | MODIFY | Chuyển `collection_id` thành `col_drafting` và `format` thành `docx,pdf`. |
| `frontend/src/hooks/use-rag-stream.ts` | MODIFY | Thêm trường `url` vào `ChatAttachment`, `artifacts` vào `ChatMessageItem`, parse artifacts từ SSE và API. |
| `frontend/src/components/ai/chat-message.tsx` & `attachment.tsx` | MODIFY | Render thẻ tải file `.docx` và `.pdf` trực quan có kích thước, icon màu và nút download. |

## Kết Quả Kiểm Thử
- `npm run lint`: Checked 93 files in 144ms. 0 error.
- `npm run typecheck`: `tsc --noEmit` exit 0.
- `npm run build`: Vite v6.4.3 built thành công trong 10.35s (`dist/` bundle sạch).
- `ruff check .`: All checks passed (0 error).
- `pytest tests/test_document_generator.py tests/test_assistants.py tests/test_tools.py tests/test_workflows.py -v`: 44/44 passed (100%).
- `python scripts/check_mojibake.py`: 231/231 files sạch 100%.
- `Live Verification`: Chatbot trả lời câu hỏi thể thức NĐ 30 trích dẫn chính xác Điều 7, Điều 9, hiển thị bảng số liệu Facts và tự động sinh tệp tải về.
