# MEMORY SNAPSHOT — Phiên Làm Việc #55
# Ngày: 2026-09-17 | Nội dung: Chuẩn Hóa Phân Vùng Bố Cục Vĩ Mô (Macro Layout Sections) Theo Chuẩn QNU-AI-Core

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 17:40 (UTC+7)
- Giai đoạn vừa hoàn thành:
  - Khảo sát mã nguồn thực tế `qnu-ai-core` (`smart_layout_detector.py`, `test_layout_classifier.py`, `sample-data.ts`).
  - Xác định nguyên nhân lỗi scan quá chi tiết: thiếu tầng gom cụm kề (Adjacent Merging) sau khi bóc tách vector blocks.
  - Tích hợp phương thức `_merge_into_macro_regions` vào `SmartLayoutDetector`:
    * Gom header Trang 1 thành 2 khối cân xứng (Cơ quan bên trái & Quốc hiệu bên phải).
    * Giữ tiêu đề chính độc lập.
    * Gom các đoạn căn cứ và tiểu mục chỉ đạo thành các khối text vĩ mô.
    * Bảo vệ bảng biểu, con dấu và khối nơi nhận.
  - Giảm 67% số lượng bounding boxes trên toàn bộ tài liệu (từ 36 khung vụn xuống 12 khung vĩ mô).
- Test Suite Backend: 125/125 tests passed (100%), Ruff check 0 lỗi.
- Test Suite Frontend: Biome lint 0 lỗi trên 78 files, TypeScript 0 lỗi, Vite build thành công (5.33s).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `backend/app/modules/ocr/layout_detector.py` | MODIFY | Thêm phương thức `_merge_into_macro_regions` gom cụm vĩ mô các khối chữ kề nhau, cập nhật regex nhận diện tiêu đề không dấu (`KE HOACH`, `QUYET DINH`), gọi gom cụm tại cuối `_detect_hybrid_pdf_regions`. |
| `backend/tests/test_smart_layout.py` | MODIFY | Cập nhật assertions kiểm thử phân loại bố cục vĩ mô trên trang 1 và trang 2. |

## Kết Quả Kiểm Thử
- `npm run lint`: Biome check 78 files, 0 lỗi.
- `npm run typecheck`: TypeScript tsc --noEmit 0 lỗi.
- `npm run build`: Vite build hoàn tất thành công trong 5.33s.
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest tests/ -q`: 125/125 passed (100%) in 25.49s.
