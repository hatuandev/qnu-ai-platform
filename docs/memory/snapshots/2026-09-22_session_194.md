# MEMORY SNAPSHOT — Phiên Làm Việc #194
# Ngày: 2026-09-22 | Nội dung: Khắc Phục Lỗi Index Kho Tri Thức, Ánh Xạ Mô Hình BGE-M3 & Đồng Bộ Credentials Provider Lúc Khởi Động

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-22 10:35 (UTC+7)
- Giai đoạn: Knowledge Ingestion, Vector Indexing & Provider Resilience
- Test Suite: Pytest **70/70 passed (100%)** (modelops, knowledge, rag), Ruff **0 errors**, Biome **0 errors** (170 files), TypeScript **0 errors**.

## Tóm Tắt Vấn Đề & Nguyên Nhân Gốc Rễ
Người dùng nạp tài liệu `Thong tin tuyen sinh dai hoc 2026_Lan2-1.pdf` vào Kho Tri thức Đề án Tuyển sinh (`col_admissions`), giao diện hiển thị badge màu đỏ `! Lỗi index`.
Lỗi xuất phát từ 2 nguyên nhân liên hoàn:
1. `settings.CLOUDFLARE_ACCOUNT_ID` là `None` lúc server khởi động vì thiếu cơ chế sync credentials từ bảng `model_provider_configs` vào `settings` trong `lifespan`.
2. SentenceTransformers local fallback gọi `SentenceTransformer("@cf/baai/bge-m3")` bị lỗi 404 trên Hugging Face vì tên repo chuẩn là `BAAI/bge-m3`.
3. Hệ thống kích hoạt chốt chặn Anti-Mock từ chối nạp dữ liệu giả mạo $\rightarrow$ `EMBEDDING_UNAVAILABLE` 503 và gán `index_status = "index_failed"`.

## Bảng Tóm Tắt Thay Đổi
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/rag/vector_indexer.py` | Sửa đổi | Bổ sung hàm `_resolve_local_model_name()` tự động chuyển `@cf/baai/bge-m3` thành `BAAI/bge-m3` cho SentenceTransformers; thêm `reset_embedding_model_state()`. |
| `backend/app/modules/modelops/services/provider_service.py` | Sửa đổi | Thêm phương thức `sync_active_providers_to_runtime()` tự động quét DB và nạp credentials active providers vào runtime `settings`. |
| `backend/app/modules/modelops/service.py` | Sửa đổi | Bổ sung facade method `sync_active_providers_to_runtime()`. |
| `backend/app/main.py` | Sửa đổi | Gọi `sync_active_providers_to_runtime()` trong `lifespan` startup, đảm bảo Cloudflare và các Provider khác được kích hoạt ngay khi server bật. |
| `frontend/src/pages/collection-detail-page.tsx` | Sửa đổi | Bổ sung `refetchInterval` tự động thăm dò (3s) cho danh sách tài liệu khi có tài liệu đang `indexing` hoặc `processing`. |

## Kết Quả Kiểm Thử & Nghiệp Vụ
- `uv run ruff check .`: All checks passed (0 errors).
- `uv run --extra dev pytest tests/test_modelops.py tests/test_knowledge.py tests/test_rag.py`: 70/70 passed (100%).
- `npm run lint`: Biome checked 170 files, 0 errors.
- `npm run typecheck`: TypeScript `tsc --noEmit` 0 errors.
- Cloudflare Embedding Edge: 1024-dim vector tính toán thành công trong <1s.
- Document `doc_640939cf032e`: Đã reindex thành công 16/16 chunks, `status: ready`, `index_status: indexed`.
