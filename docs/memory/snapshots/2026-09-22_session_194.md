# MEMORY SNAPSHOT — Phiên Làm Việc #194
# Ngày: 2026-09-22 | Nội dung: Giải Pháp Bóc Tách Hoàn Chỉnh Toàn Diện: Chuẩn Hóa Bảng Đa Trang, Ghép Dòng Ngắt Đôi & Triệt Tiêu Số Trang Footer

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-22 22:30 (UTC+7)
- Giai đoạn vừa hoàn thành: Tối ưu hóa toàn diện bộ bóc tách văn bản hành chính & học thuật (PDF Universal Extraction Engine), hoàn thiện chuẩn hóa bảng đa trang (1 dòng = 1 bản ghi), phục hồi dòng ngắt trang và triệt tiêu số trang footer.
- Test Suite:
  - Backend: `uv run ruff check .` (0 lỗi), `uv run --extra dev pytest` (55/55 passed, 100%)
  - Frontend: `npm run lint` (0 lỗi, 170 files), `npm run typecheck` (0 lỗi), `npm run build` (built in 9.00s)

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/normalization/table_reconstructor.py` | MODIFY | 1. Thêm ràng buộc `_merge_second_header_row` từ chối các đoạn văn bản dài (>50 ký tự) hoặc gạch đầu dòng nhầm thành subheader.<br>2. Hỗ trợ `semantic_indices` trong `_compact_task_row` ánh xạ chính xác các hàng ngắt tiếp nối (orphan continuation row) từ các bảng có cột spacer rỗng (11 cột $\rightarrow$ 7 cột) về đúng vị trí cột.<br>3. Thêm hàm `_merge_split_roman_rows` tự động gộp các chữ số La Mã bị tách ngắt dòng (`VII` + `I` $\rightarrow$ `VIII`) và loại bỏ hàng rỗng thừa.<br>4. Tiền chuẩn hóa (`clean_table_columns`) toàn bộ các bảng trước khi so khớp `schema_key` và ghép nối liên trang trong `reconstruct_multi_page_tables`. |
| `backend/app/modules/knowledge/parsers/blocks.py` | MODIFY | Thêm phân loại `footer` trong `classify_text_block` cho các số trang đơn độc ở đáy trang (`top_percent >= 88.0` và khớp `^\d{1,3}$` hoặc `Trang \d+`). |
| `backend/app/modules/knowledge/parsers/pdf_parser.py` | MODIFY | Lọc bỏ tự động các khối số trang đơn độc ở chân trang (`b_rect.y0 > height * 0.88`), triệt tiêu hoàn toàn rác số trang trước dấu phân tách trang `---`. |
| `backend/app/modules/knowledge/services/ingestion_service.py` | MODIFY | Bỏ qua khối loại `footer` trong hàm `_synthesize_page_markdown_from_blocks`. |
| `backend/tests/test_table_stitching_and_page_partition.py` | MODIFY | Bổ sung 3 unit tests mới: `test_merge_split_roman_rows`, `test_reconstruct_multi_page_tables_mixed_spacer_columns`, `test_classify_text_block_identifies_footer_page_number` (nâng tổng số lên 8/8 tests passed). |

## Kết Quả Kiểm Thử Thực Tế Trên Kế Hoạch 22 Trang (`KE HOACH TRIEN KHAI NHIỆM VỤ 2025-2026_update 16_9_2025.pdf`)
1. **Hàng 4.10 (Trang 11-12)**:
   - Cột 0: `4.10`
   - Cột 1: Nội dung nhiệm vụ ghép nối trọn vẹn chương trình IUC-QNU giai đoạn 1, dự án ECO-WIND.
   - Cột 2: `Phòng KHCN&HTQT` (Đơn vị chủ trì giữ nguyên, không bị xô lệch).
   - Cột 3: `PSU và các đơn vị liên quan` (Đơn vị phối hợp giữ nguyên).
   - Cột 6: `Báo cáo tiến độ, kết quả thực hiện các dự án`.
2. **Hàng 7.2 (Trang 17-18)**:
   - Cột 0: `7.2`
   - Cột 1: Toàn bộ nội dung Luật Nhà giáo và các chính sách lớn, không bị cắt cụt.
   - Cột 2: `Phòng HC-TH`.
   - Cột 3: `Các đơn vị liên quan`.
   - Cột 6: Sản phẩm kết quả đầy đủ (Biên bản góp ý, danh sách văn bản nội bộ, hoạt động hưởng ứng), không bị duplicate nội dung luật.
3. **Mục La Mã VIII (Trang 18)**:
   - Gộp thành công `VII` + `I` thành `VIII` nguyên vẹn trên 1 hàng duy nhất: `| VIII | Công tác thi đua, khen thưởng | | | | | |`, loại bỏ hàng trống `| I | | |...`.
4. **Hàng 8.1 (Trang 18-19)**:
   - Ghép nối trọn vẹn qua trang 18-19, Cột 2 là `Phòng TC-NS`, Cột 6 là `Quy định về công tác thi đua...`.
5. **Khử rác số trang chân trang**:
   - Khử sạch 100% các số trang lẻ loi (`2`, `4`, `5`, ..., `22`) ở đáy các trang.
6. **Tuân thủ quy tắc 1 dòng = 1 bản ghi**:
   - Mỗi hàng trong bảng Markdown là một dòng vật lý duy nhất, các ký tự xuống dòng bên trong ô được chuẩn hóa thành `<br>`.

## Chất Lượng Mã Nguồn
- `uv run ruff check .`: All checks passed (0 errors).
- `uv run --extra dev pytest`: 55 passed (100%).
- `npm run lint`: 170 files checked, 0 errors.
- `npm run typecheck`: 0 errors.
- `npm run build`: built in 9.00s.
