# MEMORY SNAPSHOT — Phiên Làm Việc #114
# Ngày: 2026-09-19 | Nội dung: Triển khai Giai đoạn 4 — Node Catalog Contract, DAG Schema Validation & Approval Inbox Vận Hành

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-19 17:25 (UTC+7)
- Giai đoạn vừa hoàn thành: Giai đoạn 4 (Khắc phục P1-07, P1-08, P2-08 theo báo cáo khoảng hở kết nối `docs/nhan_xet_khoang_ho_ket_noi_toan_app_2026-09-19.md`)
- Test Suite: Backend 228/228 passed (100%), Biome 130 files 0 lỗi, TypeScript tsc 0 lỗi, Vite build 7.41s thành công, Zero Mojibake 286/286 files.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `backend/app/modules/node_catalog/service.py` | MODIFY | Bổ sung phương thức đồng bộ `get_manifests_map()` tải toàn bộ từ điển 13 NodeManifests từ `configs/nodes/*.json` cho compiler. |
| `backend/app/modules/workflows/compiler.py` | MODIFY | Tích hợp ánh xạ Canonical Aliases (`_CANONICAL_TYPE_ALIASES`); viết `_validate_node_config_schema` kiểm tra trường bắt buộc (`required`), kiểu dữ liệu (`string`, `integer`, `number`, `boolean`, `array`, `object`), kiểm tra `enum` (bao gồm chuỗi phân tách dấu phẩy); kiểm tra trạng thái manifest (`deprecated` -> warning, `inactive` -> error). |
| `backend/tests/test_workflows.py` | MODIFY | Bổ sung 4 test cases kiểm tra schema validation và human approval endpoints: `test_compiler_validates_node_config_schema_missing_required_field`, `test_compiler_validates_node_config_schema_success`, `test_compiler_validates_node_config_type_mismatch_and_enum`, `test_workflow_approval_api_endpoints`. Toàn bộ 24/24 tests passed. |
| `frontend/src/types/workflows.ts` | MODIFY | Khai báo interface `WorkflowApproval` định kiểu dữ liệu phê duyệt checkpoint Human-in-the-loop. |
| `frontend/src/services/workflows-api.ts` | MODIFY | Thêm phương thức `listPendingApprovals()` và `decideApproval()`. |
| `frontend/src/services/workflow-runs-api.ts` | MODIFY | Bổ sung `getPendingApprovals()` và `decideApproval()`, cập nhật status whitelist `paused_for_approval`. |
| `frontend/src/components/admin/property-inspector.tsx` | MODIFY | Viết lại hoàn chỉnh Property Inspector động hỗ trợ Two-Way Binding giữa Tab Trực Quan và Tab JSON Schema; form controls tương tác theo schema thực tế (RAG, Tool, Route, LLM, Guard, Output, Approval). |
| `frontend/src/components/admin/node-catalog-drawer.tsx` | MODIFY | Khớp danh mục fallback với 13 manifests chính thức; viết hàm `extractDefaultConfig` trích xuất giá trị mặc định từ schema. |
| `frontend/src/pages/dag-canvas-page.tsx` | MODIFY | Khởi tạo `workflowConfig` từ `defaultConfig` khi thêm node từ Drawer, triệt tiêu cấu hình rỗng gây lỗi biên dịch. |
| `frontend/src/pages/runs-page.tsx` | MODIFY | Tích hợp Hộp Thư Phê Duyệt Tác Vụ (HITL Inbox) polling mỗi 8s; bổ sung thẻ checkpoint đang chờ; tích hợp Approval Decision Dialog phê duyệt/từ chối kèm lý do; bổ sung nút [Duyệt ngay] trên hàng bảng có trạng thái `paused_for_approval`. |
| `docs/quy_trinh/04_dieu_phoi_tro_ly_dag.md` | MODIFY | Thêm Mục 12 đặc tả Schema-Driven Validation cho Node Catalog & Vận hành Approval Inbox tại `/runs`. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest -v`: **228/228 passed (100%)** trong 47.93s (+4 test cases mới).
- `npm run lint`: **130 files checked, 0 lỗi**.
- `npm run typecheck`: **0 lỗi** (`tsc --noEmit`).
- `npm run build`: **Thành công trong 7.41s** (2545 modules transformed, 0 warnings).
- `python scripts/check_mojibake.py`: **286/286 files sạch UTF-8 100%**.
