# MEMORY SNAPSHOT — Phiên Làm Việc #71
# Ngày: 2026-09-17 | Nội dung: Đồng Bộ Quản Trị Trợ Lý AI Core → Platform

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 23:24:00 (GMT+7)
- Giai đoạn vừa hoàn thành: Persisted Assistant Catalog, CRUD/Seed/Bundle API và giao diện List/Create/Detail.
- Cơ sở dữ liệu: 5 trợ lý + 5 workflow chuẩn đã tồn tại trong PostgreSQL; seed chạy lại không tạo trùng.
- API thật: `GET /assistants`, `GET /assistants/admissions`, `GET /assistants/templates` đều HTTP 200.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/assistants/schemas.py` | MODIFY | DTO typed cho lifecycle 7 lớp, CRUD, seed và bundle. |
| `backend/app/modules/assistants/seeder.py` | MODIFY | Seed idempotent 5 Assistant + 5 WorkflowDefinition từ cấu hình Core. |
| `backend/app/modules/assistants/service.py` | MODIFY | CRUD, templates, import/export, chat và loại bỏ fallback mock. |
| `backend/app/modules/assistants/router.py` | MODIFY | API quản trị Assistants đầy đủ. |
| `backend/app/main.py` | MODIFY | Chạy assistant seed an toàn trong startup lifespan. |
| `backend/scripts/seed_assistants.py` | NEW | Lệnh seed rõ ràng, có thể chạy lại an toàn. |
| `backend/tests/test_assistants.py` | MODIFY | 7 ca kiểm thử persisted data, lỗi DB, idempotency, API và chat. |
| `frontend/src/services/assistants-api.ts` | NEW | API client typed cho CRUD/seed/template/bundle. |
| `frontend/src/pages/assistants-page.tsx` | NEW | Danh sách dữ liệu thật, tìm kiếm/lọc, seed và import bundle. |
| `frontend/src/pages/assistant-create-page.tsx` | NEW | Trang tạo riêng theo vòng đời 7 bước. |
| `frontend/src/pages/assistant-detail-page.tsx` | NEW | Trang chi tiết/edit riêng, guardrails, ModelOps, Chat/DAG và export bundle. |
| `frontend/src/App.tsx` | MODIFY | Deep routes `/assistants/new` và `/assistants/:code`. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest tests/test_assistants.py -q`: 7/7 passed.
- `uv run --extra dev pytest -v`: 141/142 passed; 1 lỗi ngoài phạm vi ở test OCR concurrent do response thiếu `fallback_engine`.
- `npm run lint`: 0 lỗi trên 90 files.
- `npm run typecheck`: 0 lỗi.
- `npm run build`: thành công; chỉ còn cảnh báo bundle lớn đã có từ trước.
- Browser: `/assistants`, `/assistants/admissions`, `/assistants/new` hiển thị đúng dữ liệu thật.
