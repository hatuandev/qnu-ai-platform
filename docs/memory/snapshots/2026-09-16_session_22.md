# MEMORY SNAPSHOT — Phiên Làm Việc #22
# Ngày: 2026-09-16 | Nội dung: Bổ Sung Local Providers (SentenceTransformers & Docling) và Chuẩn Hóa Kiến Trúc MinIO / PostgreSQL

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-16 15:08 (UTC+7)
- **Mục tiêu**:
  1. Cấu hình các thành phần Local chuyên trách cho kho dữ liệu theo đúng yêu cầu:
     - **SentenceTransformers Local**: mô hình `BAAI/bge-m3` 1024D chạy offline trên CPU/GPU để nhúng vector vào kho dữ liệu.
     - **Docling Local**: mô hình `docling-tableformer-local` từ IBM Research bóc tách bảng biểu và bố cục tài liệu offline.
  2. Chuẩn hóa kiến trúc lưu trữ theo đúng nguyên tắc:
     - **MinIO S3**: Lưu 100% tệp tin gốc (PDF scan, DOCX, XLSX) và file kết xuất tại bucket `qnu-ai-documents`. Cập nhật `STORAGE_DRIVER=s3` mặc định trong `config.py` và `.env`.
     - **PostgreSQL 16**: Lưu toàn bộ dữ liệu quan hệ, collections, tài liệu metadata, structured facts và chunks kèm FTS Tiếng Việt.
     - **Qdrant**: Lưu trữ vector nhúng 1024D phục vụ RAG retrieval.
  3. Kiểm thử toàn diện và xác thực E2E trực quan giao diện `/models`.
- **Test Suite**:
  - `uv run ruff check .`: 0 errors
  - `uv run --extra dev pytest -v`: 73/73 passed (100%)
  - `npm run lint`: 0 Biome errors
  - `npm run typecheck`: 0 TypeScript errors
  - Browser Verification: Hiển thị đúng 4 thẻ provider (Mistral, Cloudflare, SentenceTransformers Local, Docling Local).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp / CSDL | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `PostgreSQL (model_provider_configs)` | Cập nhật dữ liệu | Thêm 2 bản ghi provider local: `prov_sentence_transformers` (`BAAI/bge-m3`) và `prov_docling` (`docling-tableformer-local`). Tổng cộng 4 provider sạch sẽ. |
| [`backend/app/core/config.py`](file:///d:/DuAnPhanMem/qnu-ai-platform/backend/app/core/config.py) | Chỉnh sửa | Đặt `STORAGE_DRIVER: str = "s3"` mặc định, đồng bộ thông tin `S3_ACCESS_KEY=qnu_minio_admin` và `S3_SECRET_KEY=qnu_minio_secret_2026` với Docker Compose và `.env`. |
| [`backend/app/modules/modelops/schemas.py`](file:///d:/DuAnPhanMem/qnu-ai-platform/backend/app/modules/modelops/schemas.py) | Chỉnh sửa | Thêm `sentence_transformers` và `docling` vào danh mục `PROVIDER_PRESETS`. |
| [`frontend/src/services/api-client.ts`](file:///d:/DuAnPhanMem/qnu-ai-platform/frontend/src/services/api-client.ts) | Chỉnh sửa | Bổ sung `sentence_transformers` và `docling` vào union type `ModelProvider["type"]`. |
| [`frontend/src/pages/modelops-page.tsx`](file:///d:/DuAnPhanMem/qnu-ai-platform/frontend/src/pages/modelops-page.tsx) | Chỉnh sửa | Bổ sung gợi ý mô hình cho `sentence_transformers` (`BAAI/bge-m3`) và `docling` (`docling-tableformer-local`) vào `PRESET_SUGGESTED_MODELS`. |

## Kết Quả Kiểm Thử
- Backend Ruff: 0 errors
- Backend Pytest: 73 passed, 4 warnings in 4.54s
- Frontend Biome: 0 errors
- Frontend TypeScript: 0 errors
- Browser Subagent: Xác nhận trực quan 4 thẻ hiển thị đầy đủ và chuẩn xác
