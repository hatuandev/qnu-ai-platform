# MEMORY SNAPSHOT — Phiên Làm Việc #89
# Ngày: 2026-09-18 | Nội dung: Triển Khai Đợt 1: Truthfulness & Real Runtime (Xóa bỏ hoàn toàn "Thế giới ảo")

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-18 16:32 (UTC+7)
- **Giai đoạn vừa hoàn thành**: Đợt 1 — Truthfulness & Real Runtime theo báo cáo nhận xét tổng thể `docs/nhan_xet_tong_the_qnu_ai_platform_2026-09-18.md`.
- **Test Suite**:
  - `npm run lint`: 93 files checked, 0 errors.
  - `npm run typecheck`: 0 errors.
  - `npm run build`: Thành công (built in 10.17s).
  - `uv run ruff check .`: All checks passed (0 errors).
  - `uv run --extra dev pytest -v`: 174 passed, 0 failed (100%).
  - `python scripts/check_mojibake.py`: 231 files scanned, 0 mojibake.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/rag/service.py` | MODIFY | Gọi `modelops_service.generate()` để sinh câu trả lời RAG thật với Zero-Hallucination prompt instruction, bảo lưu trích dẫn và grounded fallback an toàn. |
| `backend/app/modules/rag/retriever.py` | MODIFY | Thêm tìm kiếm từ vựng `search_sparse_fts()` qua PostgreSQL Full-Text Search (`tsvector`, `plainto_tsquery`, `ts_rank_cd`), kết hợp RRF k=60 với dense vectors. |
| `backend/app/modules/assistants/router.py` | MODIFY | Bổ sung endpoint `/chat_stream` và tham số `stream=true` trên `/chat` trả về Server-Sent Events (SSE) thật. |
| `backend/app/modules/assistants/service.py` | MODIFY | Thêm hàm `chat_stream(...)` phát SSE các event chuẩn: `status`, `citation`, `artifact`, `token`, `done`, `error`. |
| `backend/app/modules/modelops/service.py` | MODIFY | Thêm `generate_stream(...)` trả về async generator token streaming từ provider LLM. |
| `backend/app/modules/modelops/schemas.py` | MODIFY | Bổ sung `preferred_provider_id` và `preferred_model_name` vào `LLMGenerateRequest`. |
| `backend/app/modules/assistants/runtime.py` | MODIFY | Sửa `build_runtime_profile` an toàn với cả `dict` và `AssistantLifecycleConfig`. |
| `backend/app/modules/evaluation/service.py` | MODIFY | `run_evaluation` gọi trợ lý/RAG thật trước khi fallback benchmark; `get_summary_metrics` và `get_gap_inbox` truy vấn bảng `evaluation_runs` thật qua `AsyncSession`. |
| `backend/app/modules/evaluation/router.py` | MODIFY | Bổ sung endpoint `GET /runs` và truyền `db: AsyncSession` vào `get_summary_metrics` và `get_gap_inbox`. |
| `backend/tests/test_rag.py` | MODIFY | Thêm test kiểm tra RAG gọi ModelOps LLM và hermetic isolation chống stale Redis cache; 10/10 tests pass. |
| `backend/tests/test_assistants.py` | MODIFY | Thêm test SSE streaming endpoint `/chat_stream`; 10/10 tests pass. |
| `backend/tests/test_evaluation.py` | MODIFY | Thêm test cho các endpoint evaluation, run, metrics, gap inbox; 7/7 tests pass. |
| `frontend/src/hooks/use-rag-stream.ts` | MODIFY | Xóa hoàn toàn `MOCK_ASSISTANT_DATA` và `streamSimulatedText`. Đọc SSE thật từ backend và bóc tách token/citations/artifacts/done/error. |
| `frontend/src/services/api-client.ts` | MODIFY | Xóa toàn bộ mock data fallback nghiệp vụ (`MOCK_DOCUMENTS`, `MOCK_INGESTION_TASKS`, `MOCK_PROVIDERS`, `MOCK_QUOTA`, `MOCK_TOOLS`). Bổ sung `getEvaluationRuns`, `runEvaluation`, `total_evaluations` vào interface. |
| `frontend/src/pages/evaluation-page.tsx` | MODIFY | Viết lại toàn bộ trang: hiển thị bảng lịch sử chạy thật, thanh tiến trình tỷ lệ thật theo số liệu DB, huy hiệu TM-08 tự động theo ngưỡng, EmptyState khi chưa có dữ liệu, nút "Chạy Benchmark TM-08". |
| `frontend/src/pages/dashboard-page.tsx` | MODIFY | Chuẩn hóa gọi `apiClient.getTokenQuota()`. |

## Kết Quả Kiểm Thử
- `npm run lint`: Biome check src --write (93 files checked, 0 errors).
- `npm run typecheck`: tsc --noEmit (0 errors).
- `npm run build`: vite build thành công (10.17s).
- `uv run ruff check .`: All checks passed.
- `uv run --extra dev pytest -v`: 174 passed, 0 failed (100%).
- `python scripts/check_mojibake.py`: 231 files checked, 0 errors.
