# MEMORY SNAPSHOT — Phiên Làm Việc #112
# Ngày: 2026-09-19 | Nội dung: Triển Khai Giai Đoạn 2 (Khóa An Toàn API Quản Trị & Kết Nối Tool Gateway trong Workflow)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-19T16:15:00+07:00
- Giai đoạn vừa hoàn thành: **Giai đoạn 2** trong lộ trình khắc phục khoảng hở hệ thống (`docs/nhan_xet_khoang_ho_ket_noi_toan_app_2026-09-19.md`):
  + P0-01 (API Security & Dev Access Gate): Toàn bộ API quản trị đã được bảo vệ bằng `Depends(get_current_actor)`, endpoint chat của Trợ lý AI và widget nhúng (`POST /assistants/{reference}/chat`) được giữ Public; cookie phiên được cấu hình `secure=settings.ENVIRONMENT not in ("development", "test")`.
  + P0-04 (Workflow API Caller Node ↔ Tool Gateway & HITL): `APICallerNodeHandler` được refactor gọi qua `ToolService.execute_tool`, kiểm tra allowlist `assistant_profile.tools.enabled_tools`, hỗ trợ dừng workflow an toàn `paused_for_approval` khi gặp công cụ side-effect (`export_administrative_document`, `export_exam_matrix`) chưa qua duyệt, và tự động ghi log kiểm toán bất biến `ToolExecutionLog`.
  + P0-02 (ModelOps Secret Masking): Loại bỏ rò rỉ API key thô trong API danh sách cấu hình Provider (`service.py:556`), chỉ trả `api_key_masked` và các key đã sanitize.
- Test Suite: 222/222 passed (100%), Biome 0 lỗi (130 files), tsc 0 lỗi, Vite build 7.77s, Zero Mojibake 286/286 files.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/auth/dependencies.py` | MODIFY | Cập nhật `get_current_actor` đọc `get_settings()` động, raise 401 khi token không hợp lệ, và hỗ trợ header `x-enforce-auth` phục vụ kiểm thử an ninh. |
| `backend/app/modules/auth/router.py` | MODIFY | Sửa cookie `secure=settings.ENVIRONMENT not in ("development", "test")`. |
| `backend/app/modules/assistants/router.py` | MODIFY | Tách `chat_router` riêng cho endpoint `POST /{reference}/chat` để giữ trạng thái Public cho Web Widget & thí sinh/sinh viên. |
| `backend/app/modules/assistants/__init__.py` | MODIFY | Export `assistant_chat_router`. |
| `backend/app/main.py` | MODIFY | Mount `assistant_chat_router` và `auth_router` công khai; áp dụng `dependencies=[Depends(get_current_actor)]` cho toàn bộ 11 router quản trị còn lại. |
| `backend/app/modules/tools/builtin/base.py` | - | Interface chuẩn `requires_approval`. |
| `backend/app/modules/tools/builtin/document_exporter.py` | MODIFY | Bật `requires_approval = True` (side-effect xuất tệp Word NĐ 30). |
| `backend/app/modules/tools/builtin/exam_matrix_tool.py` | MODIFY | Bật `requires_approval = True` (side-effect xuất tệp Excel Bloom). |
| `backend/app/modules/tools/service.py` | MODIFY | Khởi tạo singleton `tool_service = ToolService()`; hỗ trợ hasattr `config` khi kiểm tra record trợ lý. |
| `backend/app/modules/tools/__init__.py` | MODIFY | Export `tool_service`. |
| `backend/app/modules/workflows/nodes/api_caller_node.py` | MODIFY | Refactor gọi qua `tool_service.execute_tool`, xử lý HITL `paused_for_approval`, kiểm tra allowlist trợ lý và ghi log kiểm toán. |
| `backend/app/modules/modelops/service.py` | MODIFY | Xóa `"api_key": c.api_key_encrypted` trong `list_providers`, ngăn rò rỉ secret key thô. |
| `backend/tests/conftest.py` | MODIFY | Đặt `os.environ["ENVIRONMENT"] = "test"` và xóa cache settings cho test suite. |
| `backend/tests/test_auth.py` | MODIFY | Thêm test cases: 401 khi không auth, 401 khi token sai, 200 khi có session, và chat public không bị chặn 401. |
| `backend/tests/test_workflows.py` | MODIFY | Thêm test cases: HITL `paused_for_approval`, chạy thành công khi `is_approved=True`, và kiểm tra assistant allowlist. |
| `docs/quy_trinh/04_dieu_phoi_tro_ly_dag.md` | MODIFY | Đồng bộ mục 10.1 ghi nhận quy trình điều phối an toàn qua Tool Gateway, HITL checkpoint và audit log. |

## Kết Quả Kiểm Thử
- npm run lint: Checked 130 files in 157ms. No fixes applied (0 lỗi).
- npm run typecheck: tsc --noEmit thành công (0 lỗi).
- npm run build: Vite build hoàn tất trong 7.77s (0 errors, 0 warnings).
- uv run ruff check .: All checks passed! (0 lỗi).
- uv run --extra dev pytest: 222 passed, 34 warnings in 43.56s (100% pass, +4 tests mới).
- python scripts/check_mojibake.py: Quét 286 tệp, 100% UTF-8 sạch, không phát hiện lỗi Mojibake.
