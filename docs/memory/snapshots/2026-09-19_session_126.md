# MEMORY SNAPSHOT — Phiên Làm Việc #126
# Ngày: 2026-09-19 | Nội dung: Triển Khai Giai Đoạn 4: Evaluation Trung Thực & Quality Gate AI (Đợt 7)

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-19 22:15
- **Giai đoạn vừa hoàn thành**: Giai đoạn 4 theo Kế Hoạch 07 (`07_ke_hoach_cai_thien_toan_dien_sau_code_review.md` - Đợt 7).
- **Test Suite**:
  - Backend: `ruff` 0 lỗi; `pytest` 257/257 passed (100%).
  - Frontend: `npm run lint` (Biome 164 files 0 lỗi); `npm run typecheck` (0 lỗi); `npm run build` (thành công trong 7.55s).
  - Clean Text: 320/320 files không có Mojibake.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/evaluation/models.py` | Chỉnh sửa | Thêm `evaluation_method` trên `EvaluationRun`; thêm `execution_path`, `is_refusal`, `reasoning` trên `EvaluationResultItem`. |
| `backend/alembic/versions/20260919_evaluation_items_and_method_sync.py` | Tạo mới | Migration Alembic an toàn bổ sung các cột mới cho evaluation runs & result items. |
| `backend/app/modules/evaluation/schemas.py` | Chỉnh sửa | Thêm `evaluation_method` vào `EvaluationRunRequest`/`EvaluationRunResponse`, mở rộng `EvaluationResultItemResponse`, tạo `EvaluationRunDetailResponse`. |
| `backend/app/modules/evaluation/evaluator.py` | Chỉnh sửa | Xây dựng kiến trúc đa chế độ: `BaseTM08Evaluator`, `HeuristicTM08Evaluator` (phạt refusal không hợp lệ, thưởng mutual refusal), `LLMJudgeTM08Evaluator` (chấm qua LLM rubric QNU và sinh reasoning), và factory `get_evaluator()`. |
| `backend/app/modules/evaluation/service.py` | Chỉnh sửa | Bổ sung lưu vết chi tiết từng câu hỏi vào bảng `evaluation_result_items` khi chạy benchmark; thêm `get_run_detail()` và `get_run_items()`. |
| `backend/app/modules/evaluation/router.py` | Chỉnh sửa | Thêm endpoints `GET /evaluation/runs/{run_id}` và `GET /evaluation/runs/{run_id}/items`. |
| `backend/app/modules/assistants/readiness.py` | Chỉnh sửa | Nâng cấp `_check_evaluation()` truy vấn latest run thật của trợ lý, hiển thị phương pháp đánh giá (`heuristic`/`llm_judge`), số case đạt chuẩn, và tính điểm phạt readiness minh bạch. |
| `backend/tests/test_evaluation_truthful.py` | Tạo mới | Bộ 7 unit tests chuyên sâu kiểm tra: lưu vết result items, API run detail & items, phạt refusal khi có ground truth, pass khi mutual refusal, LLM judge rubric, readiness evaluation check, và evaluator factory. |
| `frontend/src/types/evaluation.ts` | Chỉnh sửa | Mở rộng kiểu dữ liệu `EvaluationRunItem`, `EvaluationRunRequest`, `EvaluationResultItem`, và `EvaluationRunDetail`. |
| `frontend/src/services/evaluation-api.ts` | Chỉnh sửa | Thêm `getDatasets()`, `getRunDetail()`, và `getRunItems()`. |
| `frontend/src/components/evaluation/run-benchmark-dialog.tsx` | Tạo mới | Modal khởi chạy benchmark TM-08: chọn Trợ lý AI, Bộ dữ liệu Benchmark, Số lượng câu hỏi mẫu (5, 10, 20, Toàn bộ), Phương pháp đánh giá (`heuristic` hoặc `llm_judge`). |
| `frontend/src/components/evaluation/evaluation-run-detail-sheet.tsx` | Tạo mới | Sheet xem chi tiết đợt đánh giá: KPI tổng quan, bộ lọc Trạng thái (Tất cả / Đạt chuẩn / Chưa đạt), drill-down từng câu hỏi kèm Ground Truth, Phản hồi Trợ lý, Ngữ cảnh RAG, Điểm 3 tiêu chí TM-08 và Lý do/Nhận xét thẩm định. |
| `frontend/src/pages/evaluation-page.tsx` | Chỉnh sửa | Tích hợp nút "Chạy Benchmark TM-08", cột Phương pháp đánh giá & Thao tác (Xem chi tiết), tương tác nhấp mở `EvaluationRunDetailSheet`, đồng thời khôi phục xử lý `resolveGapMutation`. |
| `docs/nhat_ky/2026-09-19_phien_126_trien_khai_giai_doan_4_evaluation_quality_gate.md` | Tạo mới | Nhật ký chi tiết phiên làm việc #126. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: All checks passed! (0 errors)
- `uv run --extra dev pytest tests/test_evaluation_truthful.py`: 7 passed in 2.99s (100%)
- `uv run --extra dev pytest`: 257 passed (100%)
- `npm run lint`: Checked 164 files in 139ms. No fixes applied. (0 errors)
- `npm run typecheck`: tsc --noEmit (0 lỗi)
- `npm run build`: tsc -b && vite build hoàn tất thành công trong 7.55s.
- `python scripts/check_mojibake.py`: 320/320 files passed!
