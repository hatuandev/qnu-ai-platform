# MEMORY SNAPSHOT — Phiên Làm Việc #124
# Ngày: 2026-09-19 | Nội dung: Triển Khai Giai Đoạn 2: Database & Cross-Store Integrity, RAG Hybrid Hardening & Groundedness, ModelOps Dynamic Fallback & Usage Accounting

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-19 20:45
- **Giai đoạn vừa hoàn thành**: Giai đoạn 2 theo Kế Hoạch 07 (`07_ke_hoach_cai_thien_toan_dien_sau_code_review.md`).
- **Test Suite**:
  - Backend: `ruff` 0 lỗi; `pytest` 245/245 passed (100%).
  - Frontend: `npm run lint` (Biome 162 files 0 lỗi); `npm run typecheck` (0 lỗi); `npm run build` (thành công trong 7.60s).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/models.py` | Chỉnh sửa | Thêm `ForeignKey("knowledge_documents.id", ondelete="CASCADE")` và quan hệ cascade ORM cho `KnowledgeFact`. |
| `backend/alembic/versions/20260919_facts_foreign_key_and_schema_sync.py` | Tạo mới | Migration Alembic an toàn tạo constraint khóa ngoại cho facts và đồng bộ cột `document_type_code`. |
| `backend/app/main.py` | Chỉnh sửa | Gỡ bỏ toàn bộ các câu lệnh raw SQL `ALTER TABLE IF EXISTS ...` trong lifecycle startup. |
| `backend/app/modules/rag/facts.py` | Chỉnh sửa | Loại bỏ orphan facts (`KnowledgeDocument.id.is_(None)`), chuyển sang `join(KnowledgeDocument)` với status `["approved", "ready"]`, thêm tenant/workspace isolation. |
| `backend/app/modules/rag/vector_indexer.py` | Chỉnh sửa | Bọc `self.embed_texts` trong `try...except` để bảo vệ callers, trả về `[]` khi embedding service offline. |
| `backend/app/modules/rag/retriever.py` | Chỉnh sửa | Sửa lỗi `UnboundLocalError` trong sparse FTS, chuẩn hóa status filtering, thêm fail-safe `_safe_dense_search` chuyển sang Degraded Mode. |
| `backend/app/modules/modelops/service.py` | Chỉnh sửa | Ưu tiên `fallback_model_name` trong `_match_score`, đánh dấu `is_fallback = True`, bổ sung Quota pre-check và ghi nhận `LLMUsageLog` kèm trừ quota trong `generate_stream()`. |
| `backend/tests/test_rag.py` | Chỉnh sửa | Thêm 2 test cases: `test_sparse_fts_handles_empty_or_short_query_without_unbound_local` và `test_search_dense_gracefully_degrades_when_embedding_fails`. |
| `backend/tests/test_modelops.py` | Chỉnh sửa | Thêm 2 test cases: `test_generate_stream_records_usage_and_deducts_quota` và `test_generate_prioritizes_fallback_model_when_primary_unavailable`. |
| `docs/nhat_ky/2026-09-19_phien_124_trien_khai_giai_doan_2_database_rag_modelops.md` | Tạo mới | Nhật ký chi tiết phiên làm việc #124. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: All checks passed!
- `uv run --extra dev pytest -v`: 245 passed, 47 warnings in 45.90s (100%)
- `npm run lint`: Checked 162 files in 148ms. No fixes applied.
- `npm run typecheck`: tsc --noEmit (0 lỗi)
- `npm run build`: vite build production hoàn tất thành công trong 7.60s.
