# MEMORY SNAPSHOT — Phiên Làm Việc #35
# Ngày: 2026-09-17 | Nội dung: Khắc Phục Triệt Để Hiển Thị Scan Thật & Render Markdown Studio Đồng Bộ Với QNU-AI-Core

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 09:30 (UTC+7)
- Mục tiêu: Đồng bộ 100% tài nguyên ảnh scan 14 trang thực tế từ `qnu-ai-core`, loại bỏ HTML giả lập, render Markdown chuẩn GFM với bảng biểu 53 ngành tuyển sinh và điểm chuẩn, bổ sung chế độ xem trước khi sửa tay, đảm bảo 0 lỗi lint, 0 lỗi type, test pass 100%.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `frontend/public/ocr-cache/doc_ts_2026/page_1.jpg` -> `page_14.jpg` | Thêm mới | 14 tệp ảnh scan thực tế 300 DPI từ kho cache của `qnu-ai-core` |
| `frontend/src/services/verification-data.ts` | Tạo mới | Tách dữ liệu bóc tách 14 trang (1,393 dòng, 69KB) thành tệp độc lập |
| `frontend/src/services/api-client.ts` | Chỉnh sửa | Bổ sung `image_url` cho trang bóc tách, import `MOCK_VERIFICATION_DOCUMENT`, xóa bỏ mock inline |
| `frontend/src/components/admin/document-bounding-visualizer.tsx` | Chỉnh sửa | Dùng thẻ `<img>` ảnh scan thật, zoom/fit page, sửa type check cho `box.type` |
| `frontend/src/pages/document-verification-studio-page.tsx` | Chỉnh sửa | Tích hợp `ReactMarkdown` + `remarkGfm`, custom table styling, chế độ xem trước khi sửa tay |
| `frontend/tests/e2e/09_knowledge_ingestion_studio.spec.ts` | Cập nhật | Kiểm thử thẻ ảnh scan thật, bảng markdown table, lật 3 trang liên tiếp, pass 3/3 |
| `backend/app/main.py` | Chỉnh sửa | Dời sanitize `no_proxy` xuống sau imports chuẩn để ruff check pass 100% |
| `docs/memory/PROJECT_CONTEXT.md` | Cập nhật | Ghi nhận trạng thái phiên 35 |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: All checks passed! (0 lỗi)
- `npm run lint`: Checked 73 files, 0 errors, 0 warnings.
- `npm run typecheck`: 0 errors.
- `npm run build`: built in 11.13s (dist bundle hoàn chỉnh).
- `npx playwright test tests/e2e/09_knowledge_ingestion_studio.spec.ts --project="Google Chrome"`: 3/3 passed (18.6s).
