# MEMORY SNAPSHOT — Phiên Làm Việc #111
# Ngày: 2026-09-19 | Nội dung: Triển khai Giai đoạn 1 khắc phục khoảng hở kết nối (Widget chat_stream, Contract Quota, Facts Lifecycle)

## Trạng Thái Tại Thời Điểm Snapshot

- Thời gian: 2026-09-19 15:55 (UTC+7)
- Giai đoạn vừa hoàn thành: Triển khai Giai đoạn 1 theo báo cáo rà soát kết nối `docs/nhan_xet_khoang_ho_ket_noi_toan_app_2026-09-19.md`.
- Phạm vi thay đổi: Frontend Web Chat Widget, mã nhúng trên trang Channels và Assistant Detail, Service ModelOps Quota, ModelOps Router, Fact Layer RAG và Unit tests.
- Backend Suite: **218/218 passed (100%)** (+2 unit tests mới).
- Frontend Suite: Biome 0 lỗi (130 files), TypeScript `tsc --noEmit` 0 lỗi, Vite build thành công 8.03s.
- Zero Mojibake: 286/286 files sạch UTF-8 100%.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `frontend/public/embed/qnu-chat-widget.js` | Chỉnh sửa | Sửa endpoint từ `/chat_stream` sang `/chat`, gửi `stream: true`, chuẩn hóa `API_BASE` đọc từ `data-api-base` và xử lý SSE delta/error (P0-06) |
| `frontend/src/pages/channels-page.tsx` | Chỉnh sửa | Tự động thêm `data-api-base="${originUrl}"` vào snippet mã nhúng web widget (P0-06) |
| `frontend/src/pages/assistant-detail-page.tsx` | Chỉnh sửa | Tự động thêm `data-api-base="${originUrl}"` vào snippet mã nhúng web widget modal (P0-06) |
| `frontend/src/services/modelops-api.ts` | Chỉnh sửa | Sửa URL gọi từ `/models/quota?tenant_id=...` sang `/modelops/quotas/${tenantId}` khớp backend router (P1-14) |
| `backend/app/modules/modelops/router.py` | Chỉnh sửa | Bổ sung endpoint alias `GET /quota?tenant_id=...` tương thích ngược (P1-14) |
| `backend/app/modules/rag/facts.py` | Chỉnh sửa | Bổ sung `outerjoin` với `KnowledgeDocument` và lọc facts theo vòng đời: loại trừ 100% tài liệu `pending` và `archived` (P0-05) |
| `backend/tests/test_modelops.py` | Chỉnh sửa | Bổ sung `test_api_get_quota_endpoints` kiểm thử cả path param và query param alias |
| `backend/tests/test_rag.py` | Chỉnh sửa | Bổ sung `test_fact_layer_filters_by_document_approval_lifecycle` kiểm thử outerjoin và lifecycle filter |
| `docs/quy_trinh/03_hybrid_rag_truy_xuat.md` | Chỉnh sửa | Cập nhật Bước 3 tài liệu quy trình ghi nhận ràng buộc vòng đời tài liệu cho Structured Fact Layer |

## Kết Quả Kiểm Thử

- `uv run ruff check .`: All checks passed (0 lỗi).
- `uv run --extra dev pytest -v`: 218/218 passed (100%) trong 56.50s.
- `npm run lint`: Biome checked 130 files in 140ms, 0 lỗi.
- `npm run typecheck`: `tsc --noEmit` hoàn tất, 0 lỗi.
- `npm run build`: Vite build hoàn tất trong 8.03s, 0 warnings.
- `python scripts/check_mojibake.py`: 286/286 tệp UTF-8 sạch 100%.
