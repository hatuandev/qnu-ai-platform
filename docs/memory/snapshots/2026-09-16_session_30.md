# MEMORY SNAPSHOT — Phiên Làm Việc #30
# Ngày: 2026-09-16 | Nội dung: Live Ingestion Stepper Tracker & Rich Document Chunks Inspector (Chuẩn QNU AI Core Hướng 1)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-16 17:40 (UTC+7)
- Giai đoạn vừa hoàn thành: Triển khai trọn vẹn Hướng 1:
  1. Live Ingestion Stepper Tracker (`IngestionProgressModal`) hiển thị thanh tiến trình động 0% -> 100% qua 4 chặng (`MinIO S3` -> `OCR` -> `Chunking` -> `Vector Indexing & FTS`) kèm thời gian elapsed ms từng bước và nút hành động chuyển đổi bảng dữ liệu.
  2. Rich Document & Chunks Inspector với 3 Tabs (`chunks`, `facts`, `meta`), hỗ trợ sao chép chunk text, đối soát bảng số liệu Markdown Table chuẩn và siêu dữ liệu kỹ thuật MinIO/Qdrant.
  3. High-Fidelity Mock Provider Fallback trong `api-client.ts` hỗ trợ hoạt động kiểm thử độc lập cả khi backend offline.
- Test Suite: 5/5 Playwright E2E tests passed (20.5s) | Biome: 0 errors | TypeScript: 0 errors | Vite build: Thành công (1,105.54 kB) | Backend pytest: 73/73 passed (100%).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `frontend/src/components/admin/ingestion-progress-modal.tsx` | **NEW** | Component Modal Stepper 4 chặng theo dõi thời gian thực tiến trình bóc tách tri thức, thanh phần trăm động và đo lường latency ms. |
| `frontend/src/pages/collection-detail-page.tsx` | **MODIFY** | Kích hoạt `IngestionProgressModal` khi submit form nạp; nâng cấp Modal Dialog thành Rich Inspector 3 Tabs (Ma Trận Chunks, Lớp Số Hóa, Siêu Dữ Liệu MinIO). |
| `frontend/src/pages/knowledge-page.tsx` | **MODIFY** | Tích hợp `IngestionProgressModal` vào Tab Ingest Wizard toàn cục `/knowledge`. |
| `frontend/src/services/api-client.ts` | **MODIFY** | Bổ sung seed providers hoàn chỉnh (`Docling`, `Mistral`, `BGE-M3`, `Cloudflare`) trong `MOCK_PROVIDERS` cho fallback mode. |
| `frontend/tests/e2e/05_smart_auto_recommendation.spec.ts` | **MODIFY** | Cập nhật assertion đồng bộ với Stepper Modal và kiểm tra provider options ổn định. |
| `frontend/tests/e2e/06_ingestion_stepper_and_inspector.spec.ts` | **NEW** | Suite Playwright E2E kiểm thử toàn diện Live Stepper 4 chặng và Rich Chunks Inspector 3 Tabs. |

## Kết Quả Kiểm Thử
- **npm run lint**: `Checked 61 files in 70ms. No fixes applied.` (0 lỗi Biome)
- **npm run typecheck**: `tsc --noEmit` (0 lỗi TypeScript)
- **npm run build**: `✓ built in 6.46s` (Đóng gói thành công)
- **Playwright E2E**: `5 passed (20.5s)`
  - TC-SMART-01: Auto-recommends Docling TableFormer and ClauseBasedChunker for Word .docx files (Passed)
  - TC-SMART-02: Auto-recommends Mistral OCR and Fast-path Inspector for PDF files (Passed)
  - TC-SMART-03: Full Ingestion workflow with Smart Recommendation (Passed)
  - TC-STEP-01: Live Ingestion Stepper tracks 4 pipeline stages to 100% completion (Passed)
  - TC-STEP-02: Rich Document Chunks Inspector displays 3-tab layout and copies chunk text (Passed)
- **Backend pytest**: `73 passed, 4 warnings in 5.78s` (100% pass)
- **Backend ruff**: `All checks passed!` (0 lỗi)
