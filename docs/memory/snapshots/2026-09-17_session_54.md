# MEMORY SNAPSHOT — Phiên Làm Việc #54
# Ngày: 2026-09-17 | Nội dung: Loại Bỏ Tia Quét & Chuẩn Hóa Khung Nhận Diện Scan Bố Cục Văn Bản Hành Chính (NĐ 30/2020/NĐ-CP)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 17:25 (UTC+7)
- Giai đoạn vừa hoàn thành:
  1. Loại bỏ triệt để tính năng tia quét (nút bấm, hiệu ứng animation laser beam, badge HUD) theo yêu cầu người dùng trên toàn bộ giao diện Document Verification Studio và Scan Studio.
  2. Nâng cấp bộ nhận diện bố cục `SmartLayoutDetector` kết hợp Vector PDF và Computer Vision:
     - Nhận diện toàn vẹn bảng biểu (tự động loại trừ các khối văn bản phân mảnh bên trong bảng).
     - Phân loại chuẩn xác ngữ nghĩa văn bản hành chính (Tiêu đề Quốc hiệu/Đơn vị, Tiêu đề văn bản/Mục số, Danh sách gạch đầu dòng, Khối Nơi nhận).
     - Hợp nhất hoàn chỉnh khối Dấu & Ký (`signature`): tự động bao trọn Chức danh ký (`HIỆU TRƯỞNG`), Con dấu tròn màu đỏ của Trường ĐH Quy Nhơn và Họ tên người ký (`PGS. TS. Đoàn Đức Tùng`) với dung sai mực đè (overlapping ink).
- Test Suite Backend: 125/125 tests passed (100%), Ruff check 0 lỗi.
- Test Suite Frontend: Biome lint 0 lỗi trên 78 files, TypeScript 0 lỗi, Vite build thành công (5.54s).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `frontend/src/components/admin/document-bounding-visualizer.tsx` | MODIFY | Loại bỏ nút "Tia quét", lớp overlay `animate-scan-beam`, state `isScanning` và badge HUD. |
| `frontend/src/pages/scan-studio-page.tsx` | MODIFY | Loại bỏ nút "Tia quét", lớp overlay laser beam và state `isScanning`. |
| `backend/app/modules/ocr/layout_detector.py` | MODIFY | Bổ sung chế độ Hybrid PDF (`_detect_hybrid_pdf_regions`), triệt tiêu text blocks trong bảng biểu, hạ ngưỡng mật độ con dấu tương thích (0.04), bao trọn Chức danh + Con dấu + Chữ ký PGS.TS. Đoàn Đức Tùng không trùng lặp, thêm fallback cho bản scan đen trắng. |
| `backend/app/modules/knowledge/service.py` | MODIFY | Truyền `fitz_page=page` vào `detect_layout_regions`. |
| `backend/app/modules/ocr/service.py` | MODIFY | Truyền `fitz_page=page` vào `detect_layout_regions`. |
| `backend/tests/test_smart_layout.py` | NEW | Bộ kiểm thử đơn vị cho phân loại hành chính, mật độ con dấu tương thích và hybrid layout PDF. |

## Kết Quả Kiểm Thử
- `npm run lint`: Biome check 78 files, 0 lỗi.
- `npm run typecheck`: TypeScript tsc --noEmit 0 lỗi.
- `npm run build`: Vite build hoàn tất thành công trong 5.54s.
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest tests/ -q`: 125/125 passed (100%) in 25s.
