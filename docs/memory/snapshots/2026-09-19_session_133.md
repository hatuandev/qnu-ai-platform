# MEMORY SNAPSHOT — Phiên Làm Việc #133
# Ngày: 2026-09-19 | Nội dung: Hoàn Thành Giai Đoạn D — Khôi Phục Tính Đúng Của Knowledge & RAG

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-19 23:48 (GMT+7)
- Giai đoạn vừa hoàn thành: **Giai đoạn D: Khôi Phục Tính Đúng Của Knowledge & RAG (Docs Truth, Vector Parity & RAG Retrieval Flow)**
- Test Suite Backend: 280/280 passed (100%)
- Test Suite Frontend: Biome check 0 lỗi, TypeScript 0 lỗi, Vite build thành công

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/rag/vector_indexer.py` | Cập nhật | Fail-fast validation 11 metadata fields bắt buộc (`INVALID_POINT_PAYLOAD` 400); chuyển `search_dense` sang Positive Allowlist (`is_active=True`, `is_retrievable=True`, `document_status in ["ready", "approved"]`, `tenant_id`, `workspace_id`); chuẩn hóa `point_id` sinh UUID deterministically từ `{collection_id}:{chunk_id}` |
| `backend/app/modules/rag/schemas.py` | Cập nhật | Bổ sung `tenant_id` và `workspace_id` vào `SearchRequest` và `AskRequest` |
| `backend/app/core/redis.py` | Cập nhật | Mở rộng key partition `SemanticCache`: `rag:cache:{tenant_id}:{workspace_id}:{collection_id}:{model}:{policy_version}:{hash}` với `policy_version="v1"`; `invalidate_collection` bao quát cả v1 lẫn legacy key pattern |
| `backend/app/modules/rag/service.py` | Cập nhật | Truyền `tenant_id` và `workspace_id` vào `hybrid_retriever`, `fact_layer` và `semantic_cache` |
| `backend/app/modules/knowledge/services/ingestion_service.py` | Cập nhật | `approve_document` chuẩn bị đủ 11 fields, index thành công chuyển `status="ready"` và `index_status="indexed"`, nếu lỗi giữ `status="approved"` + `index_status="index_failed"` |
| `backend/app/modules/knowledge/services/reconciliation_service.py` | Cập nhật | `reindex_document` cung cấp đủ 11 fields; `reconcile_collection` đối soát 4 tầng (DB, Qdrant, MinIO, Redis) phát hiện orphan points, legacy schema, scope mismatch |
| `backend/app/modules/knowledge/seeder.py` | Cập nhật | Seed documents chuyển `status="ready"`, `index_status="indexed"` với đủ 11 metadata fields cho Qdrant |
| `backend/app/cli.py` | Cập nhật | Thêm subcommand `knowledge reconcile [--collection-id ID] [--fix]` và `knowledge reindex [--collection-id ID] [--document-id ID]` |
| `backend/tests/test_rag_data_truth_and_lifecycle.py` | Tạo mới | 9 test cases bao phủ toàn diện: validation 11 fields, positive allowlist dense/sparse/facts, semantic cache v1 partition, document lifecycle state machine, reconciliation 4 tầng |
| `backend/tests/test_knowledge.py` | Cập nhật | Cập nhật status assertion `ready` và kiểm thử reconciliation tương thích |
| `docs/quy_trinh/02_nap_tri_thuc_minio.md` | Cập nhật | Bổ sung Document Lifecycle State Machine 7 bước, 11 Qdrant metadata fields, và CLI commands |
| `docs/quy_trinh/03_hybrid_rag_truy_xuat.md` | Cập nhật | Bổ sung Positive Allowlist Retrieval và Multi-Tenant Policy-Partitioned Semantic Cache v1 |

## Kết Quả Kiểm Thử
- `npm run lint`: 0 lỗi (Checked 164 files, 0 errors)
- `npm run typecheck`: 0 lỗi (`tsc --noEmit` passed)
- `npm run build`: Thành công (`✓ built in 9.58s`)
- `uv run ruff check .`: 0 lỗi
- `uv run --extra dev pytest -v`: 280/280 passed (100% pass)
