# MEMORY SNAPSHOT — Phiên Làm Việc #196
# Ngày: 2026-09-22 | Nội dung: Khắc Phục Triệt Để 6 Vấn Đề Cấu Trúc Trong Bộ Bóc Tách Tài Liệu (PDF, OCR, Table Reconstructor & Cleaner)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-22 14:26 (UTC+7)
- Giai đoạn vừa hoàn thành: Nâng cấp kiến trúc bộ bóc tách tài liệu (Tầng 1: `pdf_parser.py`, Tầng 2: `table_reconstructor.py`, Tầng 3: `cleaner.py`, Tầng 4: `markdown_renderer.py`). Giải quyết triệt để 6 vấn đề: đảo trật tự đọc, hàng mồ côi ngắt trang, xé bảng song song IELTS/VSTEP, header đa tầng rớt vào data row, rò rỉ rác OCR và nuốt hàng phân cách GFM table.
- Test Suite:
  - `uv run ruff check .`: All checks passed (0 lỗi)
  - `uv run --extra dev pytest tests/test_table_reconstructor.py tests/test_knowledge.py -v`: 51/51 passed (100%)
  - `python scripts/validate_admission_markdown.py`: 100% passed trên cả 2 tệp bóc tách thực tế và tệp chuẩn hóa
  - `python scripts/check_mojibake.py`: 414/414 tệp sạch, 0 lỗi Mojibake
  - `npm run lint`: 170 files checked, 0 errors
  - `npm run typecheck`: tsc --noEmit, 0 errors

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `backend/app/modules/knowledge/parsers/pdf_parser.py` | Cập nhật | Bổ sung thuật toán `_fuse_side_by_side_tables` ghép ngang bảng IELTS/VSTEP; mở rộng margin buffer `_is_table_text` triệt tiêu rác OCR; thêm guard `is_heading` bảo vệ tiêu đề mục. |
| `backend/app/modules/knowledge/normalization/table_reconstructor.py` | Cập nhật | Bổ sung `is_sub_header_row` khử sub-header lặp lại; cập nhật `_PROGRAM_CODE_RE = r"^\d{7}[A-Za-z]*$"`; thêm `_normalize_program_code_cells`; cập nhật `_forward_fill_hierarchical_columns` điền tên môn phân cấp Phụ lục 1. |
| `backend/app/modules/knowledge/cleaner.py` | Cập nhật | Thêm điều kiện `has_page_boundary` vào `_stitch_table_continuations` ngăn chặn nuốt nhầm dòng phân cách GFM `\|---|---|` bên trong bảng. |
| `backend/app/modules/knowledge/normalization/markdown_renderer.py` | Cập nhật | Sắp xếp khối văn bản và bảng theo `top_y` thực tế từng trang; sinh căn lề ngữ nghĩa `:---:` và `:---` cho bảng GFM Markdown. |
| `backend/tests/test_table_reconstructor.py` | Cập nhật | Thêm unit tests cho `_fuse_side_by_side_tables`, `is_sub_header_row`, `_normalize_program_code_cells`, `_forward_fill_hierarchical_columns`, `test_row_with_program_code_is_not_orphan`. |
| `scripts/validate_admission_markdown.py` | Cập nhật | Cập nhật regex và boundary kiểm định tự động tương thích với cả luồng raw extraction và format golden. |
| `Thong tin tuyen sinh dai hoc 2026_Lan2-1_boc_tach (1).md` | Tạo mới/Cập nhật | Tệp Markdown kết quả trích xuất tự động từ PDF 14 trang, vượt qua 100% 9 tiêu chí kiểm toán dữ liệu. |
| `docs/nhat_ky/2026-09-22_phien_196_khac_phuc_triet_de_6_van_de_bo_boc_tach_tai_lieu.md` | Tạo mới | Nhật ký chi tiết phiên làm việc #196. |

## Kết Quả Kiểm Thử
- npm run lint: 170 files checked, 0 errors
- npm run typecheck: tsc --noEmit, 0 errors
- uv run ruff check .: All checks passed! (0 errors)
- uv run --extra dev pytest: 51/51 passed (100%)
- python scripts/validate_admission_markdown.py: 100% Passed (0 errors)
- python scripts/check_mojibake.py: 414/414 files clean (0 errors)
