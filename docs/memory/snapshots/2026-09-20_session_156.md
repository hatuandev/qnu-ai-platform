# MEMORY SNAPSHOT — Phiên Làm Việc #156
# Ngày: 2026-09-20 | Nội dung: Đồng Bộ Hóa Toàn Diện Sidebar & Chuẩn Hóa Typography Scale Font-Size Theo Chuẩn QLKTX (ktx.qnu.edu.vn/dashboard)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-20 17:39 (UTC+7)
- Giai đoạn: Hoàn thiện Design System UI, đồng bộ Sidebar và nâng cấp Font-Size toàn hệ thống chuẩn QLKTX.
- Test Suite: Frontend Biome 0 lỗi, TypeScript 0 lỗi, Vite build thành công (11.08s).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `frontend/src/styles/tokens.css` | MODIFY | Tăng các token font-size: page title 32px, section title 20px, body 15px, table header 14px, metadata 12px. |
| `frontend/src/styles/globals.css` | MODIFY | Cập nhật typography scale utilities (.type-page-title, .type-section-title, .type-control, .type-table, .type-caption, .type-supporting). |
| `frontend/src/layouts/app-sidebar.tsx` | MODIFY | Tái cấu trúc DOM `<aside>` theo QLKTX; Nâng cấp nav items lên `text-sm font-medium` (14px) và icon `size-4.5`; Section header `text-xs uppercase`; Brand title `text-[15px]` & subtitle `text-xs`; User Profile footer với avatar `size-8.5`, name `text-sm font-semibold`, email `text-xs`; Nút resize handle. |
| `frontend/src/layouts/topbar.tsx` | MODIFY | Tăng breadcrumbs lên `text-sm`, search input lên `text-sm`, user profile trigger lên `text-sm font-semibold` & `text-xs`. |
| `frontend/src/components/admin/page-header.tsx` | MODIFY | Eyebrow `text-xs font-semibold uppercase`, Title `text-2xl sm:text-3xl font-bold`, Description `text-sm sm:text-base`. |
| `frontend/src/components/admin/kpi-metric.tsx` | MODIFY | Label `text-sm font-medium`, Value `text-2xl sm:text-3xl font-bold font-mono`, Helper `text-xs`. |
| `frontend/src/components/ui/button.tsx` | MODIFY | Chuẩn hóa size `sm` và `default` lên `text-sm font-medium` (14px). |
| `frontend/src/components/ui/input.tsx` | MODIFY | Chuẩn hóa size `sm` và `default` lên `text-sm` (14px). |
| `frontend/src/components/ui/card.tsx` | MODIFY | CardTitle `text-base sm:text-lg font-semibold`, CardDescription `text-sm text-muted-foreground`. |
| `frontend/src/components/ui/table.tsx` | MODIFY | TableHead `text-sm font-semibold`, TableCell `text-sm text-foreground`. |
| `frontend/src/layouts/admin-shell.tsx` | MODIFY | Truyền `onToggleSidebar`, gán `data-state` và padding `lg:pl-[var(--sidebar-width)]` trên main wrapper. |

## Kết Quả Kiểm Thử
- npm run lint: 166 files checked in 188ms, 0 errors, 0 warnings.
- npm run typecheck: 0 errors (tsc --noEmit clean).
- npm run build: ✓ built in 11.08s (2659 modules transformed).
- Zero Feature Regression: 100% routes, sub-sections, dialogs và queries hoạt động ổn định.
