# MEMORY SNAPSHOT — Phiên Làm Việc #66
# Ngày: 2026-09-17 | Nội dung: Tích hợp Smart Auto-Recommendation cho Trang Nạp Tài Liệu Kho Tri Thức

## Trạng Thái Tại Thời Điểm Snapshot

- Thời gian: 2026-09-17 22:21 (UTC+7)
- Phiên số: #66
- Giai đoạn vừa hoàn thành:
  1. Nâng cấp `frontend/src/lib/file-inspector.ts`: bổ sung nhận diện loại văn bản tiếng Việt theo 37 loại Taxonomy, trích xuất năm 4 chữ số, làm sạch tiêu đề và tính mức ưu tiên pháp lý động.
  2. Tích hợp toàn diện vào `frontend/src/pages/document-ingest-page.tsx`: tự động chọn bộ máy OCR (Docling cho Word/Excel), tự động nhận diện loại văn bản, tự động điền năm hiệu lực và hiển thị Smart Recommendation Banner trực quan.
  3. Xác thực kiểm thử: Biome lint 0 lỗi trên 86 files, TypeScript 0 lỗi, Vite build thành công (12.59s), test suite logic 100% pass.
- Database: PostgreSQL, Qdrant, Redis, MinIO, Gotenberg đang chạy healthy trên Docker; Backend Uvicorn đang chạy trên port 8001.
- Backend Test Suite: 136/136 tests passed (100% pass), Ruff check 0 lỗi.
- Frontend Build: `npm run lint` 0 lỗi (Biome), `npm run typecheck` 0 lỗi, `npm run build` đóng gói thành công.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
|---|---|---|
| `frontend/src/lib/file-inspector.ts` | Sửa | Thêm `detectDocumentTypeFromFilename`, `extractYearFromFilename`, `cleanTitleFromFilename`, `getPriorityForDocumentType`, chuẩn hóa regex `\p{M}/gu`. |
| `frontend/src/pages/document-ingest-page.tsx` | Sửa | Tích hợp tự động đề xuất cấu hình bóc tách khi chọn file: auto-fill title/OCR engine/doc type/year, hiển thị Smart Recommendation Banner và ưu tiên pháp lý động. |
| `docs/nhat_ky/2026-09-17_tich_hop_smart_auto_recommendation_ingest.md` | Tạo mới | Nhật ký chi tiết phiên #66. |
| `docs/memory/snapshots/2026-09-17_session_66.md` | Tạo mới | Snapshot bộ nhớ phiên #66. |
| `docs/memory/PROJECT_CONTEXT.md` | Sửa | Cập nhật phiên #66 và trạng thái hệ thống. |
| `docs/WORK_LOG.md` | Sửa | Cập nhật bảng tổng kết công việc. |

## Kết Quả Kiểm Thử

- `npm run lint`: 0 lỗi (Biome check 86 files).
- `npm run typecheck`: 0 lỗi (tsc --noEmit).
- `npm run build`: Vite build thành công (12.59s).
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest`: 136/136 tests passed (100%).
