# MEMORY SNAPSHOT — Phiên Làm Việc #17
# Ngày: 2026-09-16 | Nội dung: Nâng Cấp Quản Lý Provider — Presets & Multi-Key Pool Failover

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-16 11:15 (UTC+7)
- Giai đoạn vừa hoàn thành: Nâng cấp module Quản Lý Provider theo tham chiếu `qnu-ai-core`. Tích hợp cấu hình sẵn 12 Provider Presets (OpenAI, Gemini, Claude, DeepSeek, Groq, OpenRouter, Mistral, Cloudflare, NVIDIA NIM, Ollama, Local vLLM, Custom), hỗ trợ Custom Provider chuẩn OpenAI. Xây dựng trọn bộ hệ thống Nhóm Khóa API (Key Pool) cho phép lưu nhiều key/provider kèm cơ chế JIT Failover / tự động xoay vòng khóa khi gặp lỗi Rate Limit 429 hoặc cạn Token Quota.
- Test Suite Backend: 70/70 tests passed (100%)
- Test Suite E2E Frontend: 15/15 Playwright tests passed (100%)

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `backend/app/modules/modelops/schemas.py` | Sửa | Khai báo `ProviderKeyItem`, `ProviderKeyCreate`, `ProviderKeyUpdate`, `SimulateKeyRotationRequest`, `SimulateKeyRotationResponse`, `ProviderKeyTestResponse`, `ProviderPresetItem`, danh mục `PROVIDER_PRESETS`. |
| `backend/app/modules/modelops/service.py` | Sửa | Lưu trữ `api_keys` trong `extra_config`, triển khai Key Pool CRUD, tự động giải phóng cooldown 429, mô phỏng xoay key, và thuật toán multi-key rotation trong `generate()`. |
| `backend/app/modules/modelops/router.py` | Sửa | Bổ sung endpoint presets và 6 endpoint quản lý Key Pool. |
| `backend/tests/test_modelops.py` | Sửa | Bổ sung unit test presets và test CRUD Key Pool kèm rotation simulation (70/70 passed). |
| `frontend/src/services/api-client.ts` | Sửa | Khai báo `ProviderApiKey`, `ProviderPreset`, mở rộng `ModelProvider` và bổ sung 7 REST client methods. |
| `frontend/src/pages/modelops-page.tsx` | Sửa | Tích hợp carousel chọn Presets tự động điền endpoint, bổ sung badge số lượng key trên card, và modal quản lý Key Pool chuyên biệt kèm nút test mô phỏng xoay key 429. |
| `frontend/tests/e2e/04_modelops_key_pool.spec.ts` | Tạo mới | Bộ 3 test Playwright E2E cho giao diện Quản Lý Provider, Presets selection, và Key Pool. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi
- `uv run --extra dev pytest`: 70 passed, 4 warnings in 12.11s (100% pass)
- `npm run lint`: 0 lỗi (Biome check 58 files)
- `npm run typecheck`: 0 lỗi (tsc --noEmit)
- `npm run build`: Thành công (1,040 kB bundle)
- `npm run test:e2e`: 15 passed in 40.2s (100% pass)
