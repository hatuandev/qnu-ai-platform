# MEMORY SNAPSHOT — Phiên Làm Việc #72
# Ngày: 2026-09-17 | Nội dung: Tích Hợp Mistral OCR & Cơ Chế Điều Phối Bóc Tách Đa Tầng Core → Platform

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 23:28:00 (GMT+7)
- Giai đoạn vừa hoàn thành: Tích hợp `MistralOCRAdapter` (`mistral-ocr-latest` Cloud API), chuẩn hóa logic điều phối bóc tách theo đúng kiến trúc `qnu-ai-core`:
  1. PDF có text số hóa: Fast-path trích xuất tức thì với PyMuPDF (10-30ms), không qua OCR nặng.
  2. PDF dạng scan (ảnh không có text layer) & File ảnh: Ưu tiên Mistral OCR (Cloud API 1-2s). Nếu thiếu API key hoặc lỗi mạng, tự động Graceful Fallback sang Local OCR (`easyocr` / `docling` / `pymupdf_ocr`).
  3. File Office (Word `.docx`, Excel `.xlsx`): Ưu tiên Docling TableFormer / openpyxl bảo toàn 100% ma trận bảng.
  4. Sửa lỗi EasyOCR buffer 1D trong `easyocr_adapter.py`.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/ocr/adapters/mistral_adapter.py` | NEW | Adapter gọi Mistral OCR API (`mistral-ocr-latest`), kiểm tra availability qua `settings.MISTRAL_API_KEY`. |
| `backend/app/modules/ocr/adapters/__init__.py` | MODIFY | Export `MistralOCRAdapter`. |
| `backend/app/modules/ocr/adapters/easyocr_adapter.py` | MODIFY | Truyền trực tiếp `image_bytes` vào `reader.readtext` sửa lỗi buffer 1D. |
| `backend/app/modules/ocr/service.py` | MODIFY | Đăng ký engine `mistral_ocr`, triển khai smart routing theo extension và graceful local fallback khi thiếu key. |
| `backend/app/modules/knowledge/service.py` | MODIFY | Điều phối tự động bóc tách: PDF/ảnh scan chuyển OCR rescue với Mistral OCR và fallback local. |
| `backend/tests/test_ocr.py` | MODIFY | Cập nhật catalog 5 engines, thêm test fallback khi thiếu key và test routing Mistral. |
| `frontend/src/lib/file-inspector.ts` | MODIFY | Đề xuất trực quan "Fast-path + Mistral OCR" cho PDF scan và "Vision OCR • Mistral" cho ảnh. |
| `frontend/src/pages/document-ingest-page.tsx` | MODIFY | Thêm tùy chọn "Mistral OCR (Cloud Vision 1-2s)" vào dropdown bộ máy OCR. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: All checks passed (0 lỗi).
- `uv run --extra dev pytest tests/test_ocr.py -v`: 13/13 passed (100%).
- `uv run --extra dev pytest tests/test_knowledge.py -v`: 25/25 passed (100%).
- `npm run lint`: 0 lỗi trên 90 files.
- `npm run typecheck`: 0 lỗi.
- `npm run build`: Vite build thành công trong 15.95s.
