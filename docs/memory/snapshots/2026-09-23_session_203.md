# MEMORY SNAPSHOT — Phiên Làm Việc #203
# Ngày: 2026-09-23 | Nội dung: Seed Google Gemini Vision OCR, Phân Biệt Model OCR vs Text & Thiết Lập OCR Mặc Định Kho Tri Thức

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-23 11:20 (UTC+7)
- Giai đoạn vừa hoàn thành: Seed Google Gemini Provider Models & GeminiOCRAdapter Integration
- Test Suite Backend: 414/414 passed (100%)
- Test Suite Frontend: Biome 170 files 0 errors, tsc 0 errors, Vite build successful (10.96s)

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/modelops/services/provider_service.py` | Cập nhật | Bổ sung 8 mô hình Google Gemini 2.5 và 3.x, kèm `model_specs` phân loại rõ `can_ocr` vs thuần text |
| `backend/app/modules/modelops/services/model_catalog_service.py` | Cập nhật | Bổ sung các mô hình Gemini có `can_ocr` / `vision` vào danh sách `available_ocrs`, loại trừ dòng `gemma-*` |
| `backend/app/modules/ocr/adapters/gemini_adapter.py` | Tạo mới | Triển khai `GeminiOCRAdapter` bóc tách Markdown GFM từ PDF và hình ảnh scan qua Google Gemini API |
| `backend/app/modules/ocr/service.py` | Cập nhật | Đăng ký `gemini_ocr`, đưa vào candidate_order ưu tiên hàng đầu trong auto OCR và cơ chế fallback an toàn |
| `backend/tests/test_ocr.py` | Cập nhật | Bổ sung test case kiểm thử `gemini_ocr` catalog và availability properties |
| `frontend/src/components/modelops/modelops-helpers.ts` | Cập nhật | Mở rộng `getModelCapabilities` nhận diện `isOcr`, cập nhật preset danh sách model Gemini |
| `frontend/src/components/modelops/models-grid.tsx` | Cập nhật | Bổ sung filter `OCR & Bóc tách`, gắn huy hiệu `OCR` và `Text` rõ ràng trên card model |
| `frontend/src/components/modelops/system-defaults-card.tsx` | Cập nhật | Hiển thị thông báo trạng thái khi chọn Google Gemini Vision OCR làm mặc định hệ thống |
| `frontend/src/pages/document-ingest-page.tsx` | Cập nhật | Bổ sung `gemini: "gemini_ocr"` vào tham số OCR engine và hiển thị tên bộ máy trên giao diện |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest`: 414/414 passed (100%).
- `npm run lint`: 170 files, 0 lỗi.
- `npm run typecheck`: 0 lỗi.
- `npm run build`: Thành công (10.96s).
