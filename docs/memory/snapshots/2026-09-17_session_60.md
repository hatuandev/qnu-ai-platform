# MEMORY SNAPSHOT — Phiên Làm Việc #60
# Ngày: 2026-09-17 | Nội dung: Seed Provider Mistral OCR & Cloudflare Workers AI (BGE-M3 + Reranker)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 21:08 (UTC+7)
- Phiên số: #60
- Giai đoạn vừa hoàn thành: Tích hợp và seed dữ liệu Cloudflare Workers AI cho BGE-M3 Embedding và Cross-Encoder Reranker, đồng bộ Mistral OCR.
- Tốc độ BGE-M3 Embedding: 16 chunks mất **0.71s - 1.00s** trên Cloudflare Edge GPU (so với 101.94s trên local CPU, nhanh hơn 100 lần).
- Tốc độ BGE Reranker: 3 đoạn văn bản mất **1.21s - 2.89s** trên Cloudflare Reranker API.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| [`backend/app/core/config.py`](file:///d:/DuAnPhanMem/qnu-ai-platform/backend/app/core/config.py) | Sửa đổi | Bổ sung `EMBEDDING_PROVIDER="cloudflare"` và `RERANKER_PROVIDER="cloudflare"`, đặt mặc định `EMBEDDING_MODEL="@cf/baai/bge-m3"` và `RERANKER_MODEL="@cf/baai/bge-reranker-base"`. |
| [`backend/.env`](file:///d:/DuAnPhanMem/qnu-ai-platform/backend/.env) | Sửa đổi | Cấu hình `EMBEDDING_PROVIDER=cloudflare`, `EMBEDDING_MODEL=@cf/baai/bge-m3`, `RERANKER_PROVIDER=cloudflare`, `RERANKER_MODEL=@cf/baai/bge-reranker-base`, nạp đầy đủ `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_API_KEY`, `MISTRAL_API_KEY`. |
| [`backend/app/modules/rag/vector_indexer.py`](file:///d:/DuAnPhanMem/qnu-ai-platform/backend/app/modules/rag/vector_indexer.py) | Sửa đổi | Bổ sung `_embed_texts_cloudflare` gọi trực tiếp API Cloudflare Workers AI `@cf/baai/bge-m3`, hỗ trợ batching 16 chunks/lần và tự động fallback sang local SentenceTransformers hoặc deterministic mock vector khi mất kết nối. |
| [`backend/app/modules/rag/reranker.py`](file:///d:/DuAnPhanMem/qnu-ai-platform/backend/app/modules/rag/reranker.py) | Sửa đổi | Bổ sung `_rerank_cloudflare` gọi API `@cf/baai/bge-reranker-base` với định dạng `contexts: [{"text": ...}]`, gán lại điểm xếp hạng theo độ tương quan ngữ nghĩa, tự động fallback sang RRF score ordering khi có lỗi. |
| PostgreSQL `model_provider_configs` | Cập nhật | Seed và kích hoạt 2 provider `prov_cloudflare` (models `@cf/baai/bge-m3`, `@cf/baai/bge-reranker-base`) và `prov_mistral` (model `mistral-ocr-latest`), nạp khóa API vào Key Pool với tên chuẩn UTF-8 ("Khóa Mistral OCR & Platform"). |
| [`backend/tests/test_rag.py`](file:///d:/DuAnPhanMem/qnu-ai-platform/backend/tests/test_rag.py) | Cập nhật | Cập nhật ca kiểm thử `test_reranker_fallback` mô phỏng ngoại lệ dịch vụ để bảo đảm fallback RRF ordering luôn đúng. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest tests/test_rag.py`: 9/9 passed (100%).
- `uv run --extra dev pytest tests/test_knowledge.py`: 25/25 passed (100%).
- `npm run lint`: Biome check 83 files — 0 lỗi.
- `npm run typecheck`: TypeScript tsc --noEmit — 0 lỗi.
- Kiểm thử thực tế Cloudflare BGE-M3: 16 chunks hoàn thành trong **1.00s**, vector 1024 chiều.
- Kiểm thử thực tế Cloudflare Reranker: 3 ứng viên hoàn thành trong **1.21s**, điểm số ngữ nghĩa chính xác.
