# MEMORY SNAPSHOT — Phiên Làm Việc #56
# Ngày: 2026-09-17 | Nội dung: Khắc Phục Rò Rỉ Ô Bảng Song Song & Bảo Toàn Nhận Diện Danh Sách (List) Độc Lập

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 17:55 (UTC+7)
- Giai đoạn vừa hoàn thành:
  - Khắc phục sự cố trang 9 chỉ nhận diện 2 bảng và mất sạch các khối text/list:
    * Lọc triệt để chữ bên trong bảng song song (side-by-side tables) qua tính toán tỷ lệ đè diện tích dọc (>50%) và ngang (>10px) hoặc tọa độ tâm block.
    * Đặt rào cản `table_between` ngăn cách tuyệt đối việc gộp các đoạn văn bản xuyên qua bảng biểu.
    * Đổi luật gộp cụm vĩ mô sang strictly same-type (`prev["type"] == b["type"]`): `list` chỉ gộp với `list`, `text` chỉ gộp với `text`, giữ nguyên bản sắc `list`.
    * Mở rộng `_is_list_marker` hỗ trợ đầy đủ ký tự chữ cái tiếng Việt (`đ`, `Đ`), dấu ngoặc đơn, gạch đầu dòng và số thứ tự.
    * Sửa hàm `_suppress_overlapping_boxes`: chỉ triệt tiêu khối text/list khi chính nó lọt lòng trong bảng (`contain_j > 0.60`), không bao giờ xóa khối lớn vì bao quanh bảng.
  - Bổ sung ca kiểm thử `test_list_preservation_and_table_separation` trong `test_smart_layout.py`.
- Test Suite Backend: 126/126 tests passed (100%), Ruff check 0 lỗi.
- Test Suite Frontend: Biome lint 0 lỗi trên 78 files, TypeScript 0 lỗi, Vite build thành công (5.32s).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `backend/app/modules/ocr/layout_detector.py` | MODIFY | Sửa `is_inside_table` bắt trọn chữ trong 2 bảng song song; thêm rào cản `table_between`; chuẩn hóa gộp cùng kiểu để bảo toàn `list`; mở rộng `_is_list_marker` và `_is_doc_or_section_title`; sửa `_suppress_overlapping_boxes` an toàn. |
| `backend/tests/test_smart_layout.py` | MODIFY | Thêm ca kiểm thử `test_list_preservation_and_table_separation` kiểm chứng bảo toàn danh sách `b., c.`, `a), b), c)` và bảng biểu độc lập. |

## Kết Quả Kiểm Thử
- `npm run lint`: Biome check 78 files, 0 lỗi.
- `npm run typecheck`: TypeScript tsc --noEmit 0 lỗi.
- `npm run build`: Vite build hoàn tất thành công trong 5.32s.
- `uv run ruff check .`: All checks passed (0 lỗi).
- `uv run --extra dev pytest tests/ -q`: 126/126 passed (100%) in 26.62s.
