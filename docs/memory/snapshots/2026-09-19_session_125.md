# MEMORY SNAPSHOT — Phiên Làm Việc #125
# Ngày: 2026-09-19 | Nội dung: Triển Khai Giai Đoạn 3: Workflow, Tools & Multi-Turn Conversations

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-19 21:30
- **Giai đoạn vừa hoàn thành**: Giai đoạn 3 theo Kế Hoạch 07 (`07_ke_hoach_cai_thien_toan_dien_sau_code_review.md` - Đợt 6).
- **Test Suite**:
  - Backend: `ruff` 0 lỗi; `pytest` 250/250 passed (100%).
  - Frontend: `npm run lint` (Biome 162 files 0 lỗi); `npm run typecheck` (0 lỗi); `npm run build` (thành công trong 9.75s).
  - Clean Text: 318/318 files không có Mojibake.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/rag/schemas.py` | Chỉnh sửa | Thêm trường `history: list[dict[str, str]] \| None` vào `AskRequest`. |
| `backend/app/modules/rag/service.py` | Chỉnh sửa | Nạp tối đa 6 tin nhắn `history` vào giữa System Prompt và câu hỏi hiện tại trong `ask()`. |
| `backend/app/modules/workflows/nodes/rag_answer_node.py` | Chỉnh sửa | Chuyển `conversation_history` từ `context.inputs` sang `AskRequest.history`. |
| `backend/app/modules/workflows/nodes/llm_generate_node.py` | Chỉnh sửa | Nạp `conversation_history` từ `context.inputs` vào `messages` của `LLMGenerateRequest`. |
| `backend/app/modules/assistants/service.py` | Chỉnh sửa | Thêm helper `_get_recent_conversation_history()`, cập nhật `chat()` & `chat_stream()` xử lý `paused_for_approval` minh bạch (SSE event `approval_required`, `approval_id`, hướng dẫn `/runs`), nạp context history. |
| `backend/app/modules/workflows/compiler.py` | Chỉnh sửa | Thêm warning `workflow_tool_requires_approval_info` khi node `api_caller` sử dụng công cụ yêu cầu duyệt con người (`requires_approval=True`). |
| `frontend/src/hooks/use-rag-stream.ts` | Chỉnh sửa | Mở rộng `ChatMessageItem` thêm `approvalId`, `toolName`, `status: "paused_for_approval"`, bắt event `approval_required` và `done`. |
| `frontend/src/components/ai/chat-message.tsx` | Chỉnh sửa | Tích hợp HITL Approval Pending Banner phong cách Academic Teal/Amber với link trực tiếp tới `/runs`. |
| `backend/tests/test_workflow_hitl_and_history.py` | Tạo mới | Bộ 5 unit tests chuyên sâu kiểm tra HITL handshake (non-stream & SSE stream), conversation history loader, RAG history injection, và DAG compiler warning. |
| `docs/nhat_ky/2026-09-19_phien_125_trien_khai_giai_doan_3_workflow_hitl_conversation_history.md` | Tạo mới | Nhật ký chi tiết phiên làm việc #125. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: All checks passed! (0 errors)
- `uv run --extra dev pytest -v`: 250 passed, 47 warnings in 46.10s (100%)
- `npm run lint`: Checked 162 files in 150ms. No fixes applied. (0 errors)
- `npm run typecheck`: tsc --noEmit (0 lỗi)
- `npm run build`: vite build production hoàn tất thành công trong 9.75s.
- `python scripts/check_mojibake.py`: 318/318 files passed!
