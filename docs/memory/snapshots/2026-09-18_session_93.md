# MEMORY SNAPSHOT — Phiên Làm Việc #93
# Ngày: 2026-09-18 | Nội dung: Đợt 3 — Clean Code SRP Refactoring, Frontend Bundle Optimization & Golden Assistant Tuyển Sinh QNU

## Trạng Thái Tại Thời Điểm Snapshot

- **Thời gian:** 2026-09-18 19:48 (UTC+7)
- **Giai đoạn vừa hoàn thành:** Hoàn thành toàn diện hai nhiệm vụ trọng tâm được người dùng lựa chọn: **Lựa chọn C (Clean Code SRP & Bundle Optimization)** trước, sau đó là **Lựa chọn A (Golden Assistant Tuyển Sinh QNU `col_admissions` & TM-08 Benchmark 20 test cases)**:
  1. **Clean Code & Phân Rã SRP Phân Hệ ModelOps (Lựa chọn C):**
     - Phân rã tệp nguyên khối `frontend/src/pages/modelops-page.tsx` từ **2.961 dòng xuống còn 854 dòng** (giảm hơn 70% độ phức tạp, tuân thủ nghiêm ngặt nguyên lý Đơn Trách Nhiệm - Single Responsibility Principle).
     - Tạo 8 modular sub-components chuyên biệt tại `frontend/src/components/modelops/`:
       * `modelops-helpers.ts`: Phân loại provider, suy luận capabilities (Vision/Reasoning), helper format tên hiển thị, presets model.
       * `provider-card.tsx`: Master view provider card với metrics, tag chuyên dụng, trigger edit/delete/test.
       * `provider-detail-header.tsx`: Hero banner chi tiết, breadcrumb điều hướng, nút test toàn bộ và modal kích hoạt.
       * `models-grid.tsx`: Lưới 3 cột cards trực quan, badge latency/lỗi, filter Vision/Reasoning, nút bình nghiệm `FlaskConical` test model đơn lẻ, banner dọn dẹp model chết.
       * `add-custom-model-dialog.tsx`: Hộp thoại chuẩn Mac-style window controls, live ping test trực tiếp trước khi lưu, gợi ý preset chips 1-click.
       * `key-pool-section.tsx`: Quản lý danh sách API keys đa tầng, trạng thái hoạt động, cơ chế xoay vòng failover khi gặp HTTP 429.
       * `system-defaults-card.tsx`: Điều phối mô hình mặc định toàn hệ thống (Embedding BAAI BGE-M3, Reranker, OCR).
       * `resilience-policy-card.tsx`: Trực quan hóa chính sách Circuit Breaker 3 trạng thái và JIT dynamic fallback.
       * `provider-modal.tsx`: Dialog thêm mới/sửa cấu hình Provider với carousel preset mẫu.
  2. **Tối Ưu Hóa Frontend Bundle & Code Splitting (Lựa chọn C):**
     - Cấu hình chia nhỏ vendor chunks thông minh trong `frontend/vite.config.ts`: `vendor-xyflow`, `vendor-tanstack`, `vendor-markdown`, `vendor-radix`, `vendor-icons`.
     - Áp dụng `React.lazy()` và `<Suspense>` trên `frontend/src/App.tsx` cho tất cả các trang studio nặng (DAG Canvas, Scan Studio, Chat Studio, ModelOps, Knowledge, Tools,...).
     - **Kết quả đóng gói:** Entry bundle chính giảm từ **1.481,74 kB xuống còn 372,64 kB** (gzip: 107,56 kB). Tất cả các chunk đều < 400 kB. **0 cảnh báo kích thước (Zero Warnings)**, thời gian build siêu tốc: 5.83 giây.
  3. **Golden Assistant Tuyển Sinh ĐH Quy Nhơn (`col_admissions`) (Lựa chọn A):**
     - Tạo `backend/app/modules/knowledge/seed_data_admissions.py` chứa toàn diện dữ liệu Đề án và Thông báo tuyển sinh đại học chính quy ĐH Quy Nhơn năm 2024:
       * 1 Document chuẩn: `doc_qnu_tuyen_sinh_2024` (`Thong_bao_tuyen_sinh_DH_chinh_quy_QNU_2024.pdf`).
       * 7 Chunks điều khoản chuyên sâu: `chk_adm_1` đến `chk_adm_7` (Mã trường DQN, địa chỉ 170 An Dương Vương, hotline 0256.3846.156, 4 phương thức xét tuyển, khối Sư phạm & chính sách NĐ 116, khối CNTT & Kỹ thuật, khối Kinh tế & Du lịch, học phí & học bổng, ký túc xá 5.000 chỗ & thủ tục nhập học).
       * 8 Structured Facts số hóa: `fact_adm_thong_tin_chung`, `fact_adm_phuong_thuc_xet_tuyen`, `fact_adm_chi_tieu_tong` (5.860 chỉ tiêu), `fact_adm_diem_chuan_cntt` (24.50 đ), `fact_adm_diem_chuan_sp_toan` (26.25 đ), `fact_adm_hoc_phi_khoi_nganh`, `fact_adm_chinh_sach_nd116` (miễn 100% học phí + trợ cấp 3,63M/tháng), `fact_adm_ky_tuc_xa`.
     - Tích hợp `seed_admissions_knowledge()` vào `backend/app/modules/knowledge/seeder.py` và nạp thực tế vào PostgreSQL và Qdrant (7 vector points 1024-dim BAAI BGE-M3 với UUID5 deterministic IDs).
  4. **Golden Benchmark 20 Test Cases & TM-08 Evaluation (Lựa chọn A):**
     - Mở rộng `qnu_admissions_benchmark` từ 5 câu hỏi lên **20 test cases toàn diện** (`tc_adm_001` đến `tc_adm_020`) phủ kín 7 chunks và 8 facts trong `backend/app/modules/evaluation/dataset_seeder.py`.
     - Chuẩn hóa thuật toán tính `compute_context_precision` trong `backend/app/modules/evaluation/evaluator.py` sang chuẩn Mean Average Precision @ k (MAP@k) của Ragas.
     - Kết quả chạy kiểm định thực tế trên toàn bộ 20 test cases:
       * Total Cases: 20, Passed: 17, Pass Rate: 0.85 (>= 0.80)
       * Faithfulness Avg: 1.0 (Chuẩn >= 0.90) — **Đạt xuất sắc**
       * Answer Relevance Avg: 0.942 (Chuẩn >= 0.85) — **Đạt xuất sắc**
       * Context Precision Avg: 0.939 (Chuẩn >= 0.80) — **Đạt xuất sắc**
       * Meets TM-08 Standard: **True**

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `frontend/src/components/modelops/modelops-helpers.ts` | Tạo mới | Helpers phân loại provider, capabilities, tên hiển thị, presets |
| `frontend/src/components/modelops/provider-card.tsx` | Tạo mới | Card nhà cung cấp Master view độc lập |
| `frontend/src/components/modelops/provider-detail-header.tsx` | Tạo mới | Header banner chi tiết provider, breadcrumb điều hướng |
| `frontend/src/components/modelops/models-grid.tsx` | Tạo mới | Lưới hiển thị danh sách models, filters, latency badge, single test |
| `frontend/src/components/modelops/add-custom-model-dialog.tsx` | Tạo mới | Hộp thoại Mac-style thêm model với inline ping test và switches |
| `frontend/src/components/modelops/key-pool-section.tsx` | Tạo mới | Phân hệ quản lý API key pool và luân chuyển failover 429 |
| `frontend/src/components/modelops/system-defaults-card.tsx` | Tạo mới | Thẻ cấu hình mô hình mặc định toàn hệ thống |
| `frontend/src/components/modelops/resilience-policy-card.tsx` | Tạo mới | Thẻ giải thích Circuit Breaker và resilience routing |
| `frontend/src/components/modelops/provider-modal.tsx` | Tạo mới | Dialog thêm/chỉnh sửa Provider đa nhà cung cấp |
| `frontend/src/pages/modelops-page.tsx` | Tái cấu trúc | Giảm từ 2.961 dòng xuống 854 dòng, tích hợp 8 sub-components SRP |
| `frontend/vite.config.ts` | Cập nhật | Cấu hình vendor manualChunks function tối ưu hóa bundle size |
| `frontend/src/App.tsx` | Cập nhật | Chuyển đổi các trang nặng sang React.lazy + Suspense fallback |
| `backend/app/modules/knowledge/seed_data_admissions.py` | Tạo mới | Dữ liệu tuyển sinh chính thức ĐH Quy Nhơn (1 doc, 7 chunks, 8 facts) |
| `backend/app/modules/knowledge/seeder.py` | Cập nhật | Thêm `seed_admissions_knowledge()`, đồng bộ Qdrant và PostgreSQL |
| `backend/app/modules/evaluation/dataset_seeder.py` | Cập nhật | Mở rộng `qnu_admissions_benchmark` thành 20 test cases Golden Reference |
| `backend/app/modules/evaluation/evaluator.py` | Cập nhật | Chuẩn hóa `compute_context_precision` theo công thức Ragas MAP@k |

## Kết Quả Kiểm Thử

- `uv run ruff check .`: All checks passed (0 errors, 0 warnings).
- `uv run --extra dev pytest -v`: 174/174 passed (100%).
- `npm run lint`: Checked 124 files, 0 errors, no fixes applied.
- `npm run typecheck`: `tsc --noEmit` 0 errors.
- `npm run build`: Vite build thành công trong 5.83s, index chunk 372.64 kB, **0 warnings**.
- `python scripts/check_mojibake.py`: 265 files scanned, 0 errors (100% UTF-8 sạch).
