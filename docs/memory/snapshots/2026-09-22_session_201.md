# MEMORY SNAPSHOT — Phiên Làm Việc #201
# Ngày: 2026-09-22 | Nội dung: Hoàn thiện bóc tách & Reconstruct bảng đa trang cho 3 PDF nghiệp vụ (Kế hoạch 2214 Bán dẫn-AI-ANM, Kế hoạch 2781 Học liệu E-learning, Kế hoạch 4053 BĐCLGD)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-22 23:59:00+07:00
- Giai đoạn: RAG Knowledge Ingestion Pipeline Enhancement & Multi-Page Table Stitching
- Test Suite: 
  - `uv run ruff check .`: 0 lỗi (All checks passed)
  - `uv run --extra dev pytest tests/test_table_stitching_and_page_partition.py tests/test_table_reconstructor.py`: 31/31 passed in 0.97s
  - `frontend`: Biome check 170 files (0 errors), `tsc --noEmit` (0 errors)
  - End-to-end extraction trên 3 tệp PDF: 100% pass, 0 số trang rác, toàn bộ bảng được nối và giữ trọn vẹn dữ liệu.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/parsers/pdf_parser.py` | Modify | Thêm hàm `_normalize_raw_table_rows` bóc tách tiêu đề phụ lục (PHỤ LỤC, BẢNG, DANH MỤC) thành heading riêng biệt; gộp header nhiều dòng (multi-line headers) và bảo vệ dòng dữ liệu (loại trừ số STT nguyên dương, mã CTĐT, ký tự La Mã, nội dung dài > 50 ký tự khỏi header detection); cập nhật anti-leakage loại trừ administrative bullets (`- `, `+ `, `• `) để không bị chặn nhầm. |
| `backend/app/modules/knowledge/normalization/table_reconstructor.py` | Modify | Cải tiến `_has_semantic_headers` sử dụng regex word boundaries `\b...\b` và kiểm tra độ dài tối đa <= 40 ký tự để tránh false positive từ các từ viết tắt chuyên môn như `CNTT` thành `tt`; hạ ngưỡng `_normalize_spacer_columns` từ `>= 5` xuống `>= 3` và bổ sung direct remapping khi số ô dữ liệu thực tế bằng số cột tiêu đề ngữ nghĩa. |
| `backend/app/modules/knowledge/normalization/markdown_renderer.py` | Modify | Bổ sung cơ chế fallback tự động gọi `clean_table_columns` nếu bảng có cột trống trước khi huỷ bảng, đảm bảo dữ liệu hợp lệ không bao giờ bị loại bỏ oan. |
| `backend/tests/test_table_stitching_and_page_partition.py` | Modify | Bổ sung 3 unit tests mới: `test_normalize_spacer_columns_four_columns`, `test_normalize_raw_table_rows_extracts_title_and_merges`, `test_has_semantic_headers_word_boundaries`. |

## Kết Quả Kiểm Thử Thực Tế Trên 3 PDF Nghiệp Vụ
1. **File 1 (2214/KH-ĐHQN - Đề án Bán dẫn, AI & ANM - 12 trang)**:
   - Header phụ lục `PHỤ LỤC (Ban hành kèm theo Kế hoạch...)` được tách thành Heading độc lập trước bảng.
   - Bảng phụ lục đa trang 8 cột trải dài từ Trang 5 đến Trang 12 được stitch thành 1 bảng master hoàn chỉnh gồm 284 dòng (đầy đủ các phần I, II, III, IV, V, VI).
   - 0 số trang rác, 0 header trùng lặp.
2. **File 2 (2781/KH-ĐHQN - Kế hoạch Học liệu E-learning - 3 trang)**:
   - Khôi phục trọn vẹn Row 1 ("Lập danh sách học phần...") và Row 2 ("Chuẩn bị các trang thiết bị...").
   - Bảng 4 cột (`TT`, `CÔNG VIỆC`, `CHỦ TRÌ/ PHỐI HỢP`, `THỜI GIAN`) được nối liền từ Trang 1 (2 dòng) sang Trang 2 (5 dòng) thành 1 bảng master duy nhất gồm 7 dòng nhiệm vụ.
3. **File 3 (4053/KH-ĐHQN - Kế hoạch BĐCLGD - 7 trang)**:
   - Toàn bộ 20 dòng nhiệm vụ trải dài từ Trang 2 đến Trang 6 được nối thành 1 bảng master 6 cột.
   - Bảo toàn 100% các gạch đầu dòng hành chính tại Trang 6 dưới mục `III. Tổ chức thực hiện` (các gạch đầu dòng của Phòng ĐBCL&KT và các đơn vị).
