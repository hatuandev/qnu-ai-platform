# MEMORY SNAPSHOT — Phiên Làm Việc #204
# Ngày: 2026-09-23 | Nội dung: Combo OCR Dự Phòng Linh Hoạt & Tự Động Sequential Failover Khi Hết Quota (Zero-Quota-Failure Policy)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-23 12:28 (UTC+7)
- Giai đoạn vừa hoàn thành: Triển khai toàn diện Combo OCR Đa Tầng, cấu hình thứ tự ưu tiên và cơ chế Sequential Failover trên cả Backend & Frontend
- Test Suite Backend: 36/36 passed (100%) (`tests/test_ocr.py`, `tests/test_modelops.py`), Ruff 0 lỗi
- Test Suite Frontend: Biome 170 files 0 lỗi, tsc 0 lỗi, Vite build đóng gói thành công (6.49s)

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/modelops/schemas.py` | Cập nhật | Bổ sung schema `OCRComboItem`, mở rộng `SystemModelDefaults` và `SystemModelDefaultsUpdate` hỗ trợ `default_ocr_mode` và `ocr_combo_chain`, mở rộng role `fallback_ocr` / `ocr_combo` |
| `backend/app/modules/modelops/services/model_catalog_service.py` | Cập nhật | Khởi tạo chuỗi chuẩn QNU 5 bước `DEFAULT_QNU_OCR_COMBO_CHAIN` (Gemini 2.5 Flash -> Gemini Flash-Lite -> Mistral OCR -> Docling -> EasyOCR), hỗ trợ lưu trữ/cập nhật cấu hình combo trong PostgreSQL |
| `backend/app/modules/ocr/schemas.py` | Cập nhật | Bổ sung `fallback_engine: str \| None` và `cascade_trace: list[str]` vào `OCRExtractResponse` |
| `backend/app/modules/ocr/service.py` | Cập nhật | Triển khai `_get_active_combo_chain`, luồng `_extract_combo_chain` duyệt tuần tự qua các model trong chuỗi; tự động chuyển tiếp sang model kế tiếp khi gặp 429 Quota Exceeded hoặc timeout; ném RFC 7807 khi toàn bộ model fail |
| `backend/tests/test_ocr.py` | Cập nhật | Bổ sung 2 test cases kiểm thử failover tuần tự khi quota 429 và failover khi gọi engine rõ ràng thất bại |
| `backend/tests/test_modelops.py` | Cập nhật | Bổ sung assert `ocr_combo_chain` và `default_ocr_mode` trong API system defaults |
| `frontend/src/types/modelops.ts` | Cập nhật | Thêm interface `OCRComboItem` và mở rộng `SystemModelDefaults` |
| `frontend/src/components/modelops/system-defaults-card.tsx` | Nâng cấp | Thiết kế giao diện quản lý Combo OCR đa tầng: chuyển đổi chế độ Combo / Single, dải sơ đồ Visual Pipeline, bật/tắt bước, đổi thứ tự lên/xuống, thêm model từ catalog, khôi phục chuỗi QNU |
| `frontend/src/pages/document-ingest-page.tsx` | Cập nhật | Đặt `combo` làm giá trị mặc định cho bộ máy OCR, bổ sung tùy chọn "Combo OCR Mặc Định Hệ Thống", tuân thủ 100% Zero-Emoji Standard |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi (All checks passed).
- `uv run --extra dev pytest tests/test_ocr.py tests/test_modelops.py -v`: 36/36 passed (100%).
- `npm run lint`: 170 files, 0 lỗi (Checked in 68ms).
- `npm run typecheck`: 0 lỗi.
- `npm run build`: Thành công trong 6.49s.
