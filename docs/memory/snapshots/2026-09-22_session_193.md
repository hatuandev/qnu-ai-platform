# MEMORY SNAPSHOT — Phiên Làm Việc #193
# Ngày: 2026-09-22 | Nội dung: Tối Ưu Hóa Bộ Bóc Tách PDF, Phân Mảnh Bảng Đa Trang & Triệt Tiêu Lặp Text

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-22 20:15 (UTC+7)
- Giai đoạn vừa hoàn thành: Tối ưu hóa toàn diện bộ bóc tách tài liệu PDF (PyMuPDF Parser, Geometry Layout Detector, Multi-Page Table Stitching và Scan Studio).
- Test Suite: 100% passed (Pytest 27/27 tests passed trong các file liên quan, 5 unit tests mới passed 100%, Ruff 0 lỗi, Biome 170 files 0 lỗi, TypeScript 0 lỗi, Vite build thành công 11.27s).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/normalization/markdown_renderer.py` | MODIFY | Phân bổ hàng bảng đa trang theo `pnum in sorted(tbl.source_pages)`, sinh bảng Markdown độc lập kèm header cho từng trang, bảo toàn thứ tự nội dung và không để trang 3..8 bị trống. |
| `backend/app/modules/knowledge/normalization/table_reconstructor.py` | MODIFY | Cập nhật `is_orphan_continuation_row` bảo vệ các hàng có mã ngành 7 chữ số riêng biệt; tự động kế thừa tên nhóm môn `active_group` vào ô đầu trang trống của trang tiếp nối trong `reconstruct_multi_page_tables`. |
| `backend/app/modules/knowledge/parsers/blocks.py` | MODIFY | Thêm line-level clipping cho các khối văn bản giao cắt một phần với bảng (chân bảng Trang 12); sắp xếp toàn bộ khối theo tọa độ đọc tự nhiên `(y, x)` giải quyết lỗi đảo lộn thứ tự bảng/văn bản ở Trang 2 và Trang 13. |
| `backend/app/modules/knowledge/parsers/pdf_parser.py` | MODIFY | Thêm boundary clipping cho `canonical_blocks` và phân loại `BlockType.HEADING` cho các dòng tiêu đề phụ lục. |
| `backend/app/modules/knowledge/services/ingestion_service.py` | MODIFY | `build_studio_pages` hỗ trợ cả khóa `int` lẫn `str` trong `page_markdowns` (khắc phục JSONB serialization bug); đưa toàn bộ các trang có trong `page_markdowns` vào danh sách trang; `_synthesize_page_markdown_from_blocks` sắp xếp khối theo tọa độ `y`. |
| `backend/tests/test_table_stitching_and_page_partition.py` | NEW | Bổ sung 5 unit tests kiểm thử độc lập cho bảo vệ mã ngành, kế thừa header, phân bổ trang, xử lý khóa chuỗi studio pages, và sắp xếp theo tọa độ. |

## Kết Quả Kiểm Thử
- uv run --extra dev pytest tests/test_table_stitching_and_page_partition.py: 5/5 passed (100%)
- uv run --extra dev pytest tests/test_domain_records_and_quality_gate.py tests/test_document_types.py: 22/22 passed (100%)
- uv run ruff check .: All checks passed! (0 lỗi)
- npm run lint: 170 files checked, 0 lỗi
- npm run typecheck: tsc --noEmit, 0 lỗi
- npm run build: Vite build thành công (11.27s)
- Live verification trên tệp PDF tuyển sinh 14 trang: 6/6 tiêu chí đạt 100%.
