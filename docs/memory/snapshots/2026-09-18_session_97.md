# MEMORY SNAPSHOT — Phiên Làm Việc #97
# Ngày: 2026-09-18 | Nội dung: Xử lý triệt để các vấn đề P0 & P1 theo báo cáo review mã nguồn

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-18 20:55 (UTC+7)
- Giai đoạn vừa hoàn thành: Toàn bộ 4 giai đoạn xử lý P0 & P1 issues
- Backend Test Suite: **182 passed / 182 total (100% pass)**
- Ruff Lint: 0 errors
- Frontend Lint: 0 errors (124 files checked)
- Frontend Typecheck: 0 errors (`tsc --noEmit`)
- Frontend Build: Thành công

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/rag/citation_guard.py` | Modify | Thêm `ACADEMIC_STOPWORDS` tiếng Việt loại trừ false-positives; an toàn với `quote=None`; trả `[]` khi không có quote overlap. |
| `backend/app/modules/evaluation/service.py` | Modify | Xóa bỏ hoàn toàn 100% logic tự dựng câu trả lời từ `ground_truth`. Ghi nhận trung thực kết quả runtime. |
| `backend/app/modules/workflows/service.py` | Modify | Khóa cứng version execution: raise HTTP 404 `workflow_version_not_found` nếu không tìm thấy `workflow_version_id`, cấm fail-open sang draft. |
| `backend/app/modules/rag/vector_indexer.py` | Modify | Bổ sung `tenant_id`, `workspace_id`, `document_status`, `is_retrievable` vào payload; `search_dense` lọc chặt lifecycle status; thêm `count_points()`. |
| `backend/app/modules/rag/retriever.py` | Modify | Truyền và lọc theo `tenant_id` trong cả dense và sparse retrieval. |
| `backend/app/modules/rag/schemas.py` | Modify | Thêm `fallback_model` và `tenant_id` vào `AskRequest`. |
| `backend/app/modules/workflows/nodes/rag_answer_node.py` | Modify | Truyền `fallback_model` và `tenant_id` từ workflow state sang `AskRequest`. |
| `backend/app/modules/rag/service.py` | Modify | Thử sinh phản hồi bằng `fallback_model` khi primary model lỗi trước khi rơi vào raw fallback. |
| `backend/app/core/redis.py` | Modify | Nâng cấp key cache thành `rag:cache:{tenant_id}:{collection_id}:{model}:{h}` với invalidation theo wildcard. |
| `backend/app/modules/assistants/service.py` | Modify | Ép buộc `is_approved = False` từ server trong chat turns, chống client tự phê duyệt. |
| `backend/app/modules/tools/service.py` | Modify | Enforce `enabled_tools` allowlist của trợ lý và kiểm tra HITL human approval cho tools có side-effect. |
| `backend/app/modules/auth/*` | Create | Tạo module Dev Access Gate tối giản: `/login`, `/logout`, `/me`, HttpOnly session cookie `qnu_session`. |
| `backend/app/modules/knowledge/seeder.py` | Modify | Lưu trữ tệp raw text qua `storage_service` (P0.1) và tự hồi phục Qdrant points khi count=0 (P1.2). |
| `backend/tests/test_auth.py` | Create | Test suite kiểm thử Dev Access Gate login, invalid password, cookie me, logout. |
| `backend/tests/test_tools.py` | Modify | Bổ sung tests cho Assistant tool allowlist blocking và HITL approval enforcement. |
| `backend/tests/test_evaluation.py` | Modify | Cập nhật test trung thực cho evaluation run không fallback. |
| `docs/nhan_xet_sau_cai_thien_platform_2026-09-18.md` | Modify | Cập nhật bảng phân hệ: toàn bộ các điểm P0/P1 chuyển trạng thái RESOLVED. |
| `docs/quy_trinh/03_hybrid_rag_truy_xuat.md` | Modify | Bổ sung tài liệu về lifecycle filtering, tenant semantic cache và strict citation guard. |
| `docs/quy_trinh/04_dieu_phoi_tro_ly_dag.md` | Modify | Bổ sung Mục 8.4 về Dev Access Gate và bảo mật session server-side. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 errors
- `uv run --extra dev pytest -v`: **182 passed, 0 failed (100%)**
- `npm run lint`: Checked 124 files, 0 errors
- `npm run typecheck`: 0 errors
- `npm run build`: built in 12.52s, 0 errors
