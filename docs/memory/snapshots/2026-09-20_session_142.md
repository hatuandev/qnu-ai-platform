# MEMORY SNAPSHOT — Phiên Làm Việc #142
# Ngày: 2026-09-20 | Nội dung: Phân loại ngữ nghĩa khối văn bản & Khắc phục đè chữ Badge chuẩn Mistral

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-20 14:16 UTC+7
- Mục tiêu hoàn thành:
  1. Phân loại ngữ nghĩa tự động (Heuristic Classifier) cho các khối văn bản bóc tách:
     - `title` (xanh dương): số La Mã, Điều, Chương, in hoa toàn bộ, tiêu đề mục kết thúc bằng `:`
     - `list` (xanh ngọc): dấu `+`, `-`, `•`, số thứ tự danh sách `1.`, `2.`, `a)`, `b)`
     - `header` (xám): tọa độ đầu trang và chứa từ khóa hành chính
     - `signature` (hồng đỏ): tọa độ cuối trang và chứa chức danh người ký / nơi nhận
     - `text` (tím): các đoạn văn xuôi mặc định
  2. Khắc phục triệt để lỗi đè chữ của Pill Badge: Đặt `top-0 left-0 rounded-tl-[1px] rounded-br-[3px]` bên trong box, không nhô lên làm che dòng trên.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/parsers/blocks.py` | Sửa đổi | Thêm `classify_text_block` và tích hợp vào `extract_page_blocks` thay thế nhãn cứng `type: "text"` |
| `frontend/src/pages/scan-studio-page.tsx` | Sửa đổi | Bổ sung `classifyStudioRegion` trong `mapVerificationDataToStudioDoc` để dữ liệu UI hiển thị chuẩn xác ngay |
| `frontend/src/components/knowledge/ocr/ocr-canvas.tsx` | Sửa đổi | Điều chỉnh Pill Badge sang `top-0 left-0 rounded-tl-[1px] rounded-br-[3px] text-[9px]` chống đè chữ |
| `docs/nhat_ky/2026-09-20_phien_142_phan_loai_ngu_nghia_va_chong_de_chu_badge.md` | Tạo mới | Ghi chép chi tiết phiên 142 |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: Passed (0 errors)
- `uv run --extra dev pytest tests/test_knowledge.py -v`: 31 passed, 1 warning (100% pass)
- `npm run lint`: Checked 165 files, 0 errors
- `npm run typecheck`: 0 errors
- `npm run build`: vite build thành công trong 12.15s
