# MEMORY SNAPSHOT — Phiên Làm Việc #160
# Ngày: 2026-09-20 | Nội dung: Khắc phục bỏ sót hàng bảng "Đông phương học" ở đầu trang 14 (Open-Top Table Continuation Rescue)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-20 18:25
- Giai đoạn vừa hoàn thành: Khắc phục dứt điểm hiện tượng bỏ sót hàng mồ côi ở đỉnh trang ngắt trang (open-top table continuation) trên cả Backend (`blocks.py`, `pdf_parser.py`, `layout_detector.py`, `ingestion_service.py`) và Frontend (`scan-studio-page.tsx`).
- Test Suite:
  * Backend: Ruff 0 lỗi, Pytest 44/44 passed (100%).
  * Frontend: Biome 166 files / 0 lỗi, TypeScript 0 lỗi, Vite build thành công (12.34s).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `backend/app/modules/knowledge/parsers/blocks.py` | Chỉnh sửa | Bổ sung `detect_open_top_lines` quét đường kẻ dọc đỉnh trang sinh đường kẻ ngang ảo `add_lines`, bổ sung `find_page_tables` và `_rescue_open_top_table`. |
| `backend/app/modules/knowledge/parsers/pdf_parser.py` | Chỉnh sửa | Sử dụng `find_page_tables` thay cho `page.find_tables()` để bảo đảm hàng tiếp nối được bóc tách vào `CanonicalRow`. |
| `backend/app/modules/ocr/layout_detector.py` | Chỉnh sửa | Áp dụng `find_page_tables` trong `detect_layout_regions`, hạ ngưỡng `t_h >= 1.5%`. |
| `backend/app/modules/knowledge/services/ingestion_service.py` | Chỉnh sửa | Cập nhật `build_studio_pages` căn chỉnh và mở rộng BBox bảng tiếp nối bị cắt đỉnh. |
| `frontend/src/pages/scan-studio-page.tsx` | Chỉnh sửa | Mở rộng `finalTop` và `finalHeight` cho bảng tiếp nối có `top > 6.5%` và `height <= 3.5%` để bao trọn hàng đầu mồ côi. |
| `backend/tests/test_table_reconstructor.py` | Chỉnh sửa | Thêm unit test `test_detect_open_top_lines_and_rescue_table` bảo vệ không hồi quy. |

## Kết Quả Kiểm Thử
- npm run lint: Checked 166 files in 189ms, 0 errors.
- npm run typecheck: 0 errors.
- npm run build: built in 12.34s.
- uv run ruff check .: All checks passed!
- uv run --extra dev pytest tests/test_table_reconstructor.py: 13 passed in 0.93s.
- uv run --extra dev pytest tests/test_knowledge.py: 31 passed in 51.33s.
