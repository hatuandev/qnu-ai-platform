# MEMORY SNAPSHOT — Phiên Làm Việc #48
# Ngày: 2026-09-17 | Nội dung: Đồng Bộ 100% Khung Nhận Diện Scan Bố Cục Thông Minh & Con Dấu / Chữ Ký Giống QNU-AI-Core

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 23:55 (UTC+7)
- Giai đoạn vừa hoàn thành: Đồng bộ cơ chế Computer Vision Smart Layout Detection (OpenCV red stamp/signature, stale cache invalidation, API refresh layout, Frontend re-scan button)
- Test Suite Backend: 117/117 pytest passed (100%), Ruff check 0 lỗi
- Test Suite Frontend: Biome lint 0 lỗi trên 77 files, TypeScript 0 lỗi, Vite build thành công (5.56s)

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `backend/app/modules/knowledge/service.py` | MODIFY | Thêm `_is_stale_raw_blocks` nhận diện các block thô chưa phân loại; nâng cấp `_ensure_page_blocks` kích hoạt `SmartLayoutDetector` khi cache cũ hoặc khi `refresh_layout=True`; mở rộng `get_studio_view` với `refresh_layout`. |
| `backend/app/modules/knowledge/router.py` | MODIFY | Bổ sung query parameter `refresh_layout: bool = False` tại endpoint `GET /documents/{document_id}/studio-view`. |
| `frontend/src/services/api-client.ts` | MODIFY | Cập nhật `getStudioView` và `getDocumentVerification` hỗ trợ tham số `refreshLayout: boolean`. |
| `frontend/src/components/admin/document-bounding-visualizer.tsx` | MODIFY | Thêm props `isRescanning`, `onRescanLayout` và nút "Quét lại" (Smart Scan) kèm hiệu ứng xoay spinner và icon `Sparkles`. |
| `frontend/src/pages/document-verification-studio-page.tsx` | MODIFY | Tích hợp `handleRescanLayout` kích hoạt quét lại bố cục và invalidate TanStack Query cache. |
| `docs/memory/PROJECT_CONTEXT.md` | MODIFY | Cập nhật phiên làm việc #48, trạng thái kiểm thử và đồng bộ kiến trúc. |

## Kết Quả Kiểm Thử
- `npm run lint`: Biome check 77 files, 0 lỗi.
- `npm run typecheck`: TypeScript tsc --noEmit 0 lỗi.
- `npm run build`: Vite build hoàn tất thành công trong 5.56s.
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest tests/test_knowledge.py -v`: 23/23 tests passed.
- `uv run --extra dev pytest -q`: 117/117 tests passed (100%).
