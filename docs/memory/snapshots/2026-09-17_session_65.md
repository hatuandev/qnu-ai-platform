# MEMORY SNAPSHOT — Phiên Làm Việc #65
# Ngày: 2026-09-17 | Nội dung: Khắc phục lỗi "Xóa tài liệu thất bại (HTTP 500)" do Backend offline & nâng cấp xử lý lỗi proxy

## Trạng Thái Tại Thời Điểm Snapshot

- Thời gian: 2026-09-17 22:12 (UTC+7)
- Phiên số: #65
- Giai đoạn vừa hoàn thành:
  1. Xác định nguyên nhân gốc rễ lỗi HTTP 500 khi bấm xóa tài liệu: Backend (Port 8001) chưa được bật, Vite Proxy trả về 500 do ECONNREFUSED khiến UI ném lỗi 500 và fallback hiển thị mock `doc_ts_2026`.
  2. Khởi chạy và xác thực Backend Daemon FastAPI trên port 8001 (health/live, collections, defaults, jobs đều trả 200 OK).
  3. Cải tiến `apiClient.deleteDocument` trong `frontend/src/services/api-client.ts`: nhận diện lỗi proxy offline 500, dọn dẹp mock list cục bộ, hiển thị thông báo hướng dẫn người dùng rõ ràng.
- Database: PostgreSQL, Qdrant, Redis, MinIO, Gotenberg đang chạy healthy trên Docker.
- Backend Test Suite: 136/136 tests passed (100% pass), Ruff check 0 lỗi.
- Frontend Build: `npm run lint` 0 lỗi (Biome), `npm run typecheck` 0 lỗi.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
|---|---|---|
| `frontend/src/services/api-client.ts` | Sửa | Nâng cấp `deleteDocument`: xử lý thân thiện khi Backend offline (HTTP 500 ECONNREFUSED từ Vite proxy), dọn dẹp mock fallback an toàn. |
| `docs/nhat_ky/2026-09-17_khac_phuc_loi_xoa_tai_lieu_500_backend_offline.md` | Tạo mới | Nhật ký chi tiết phiên #65 phân tích lỗi và giải pháp kỹ thuật. |
| `docs/memory/snapshots/2026-09-17_session_65.md` | Tạo mới | Snapshot bộ nhớ phiên #65. |
| `docs/memory/PROJECT_CONTEXT.md` | Sửa | Cập nhật phiên #65 và trạng thái hệ thống. |
| `docs/WORK_LOG.md` | Sửa | Cập nhật nhật ký công việc chung. |

## Kết Quả Kiểm Thử

- `npm run lint`: 0 lỗi (Biome check 83 files).
- `npm run typecheck`: 0 lỗi (tsc --noEmit).
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest`: 136/136 tests passed (100%).
- Endpoint `/health/live`: HTTP 200 OK.
