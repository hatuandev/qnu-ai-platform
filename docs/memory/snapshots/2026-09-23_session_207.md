# MEMORY SNAPSHOT — Phiên Làm Việc #207
# Ngày: 2026-09-23 | Nội dung: Tối Ưu Hóa Bóc Tách PDF OCR Bằng Thuật Toán Tổng Quát & Bổ Sung Quy Chuẩn AGENTS.md

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-23 16:15 (UTC+7)
- Giai đoạn hoàn thành: Tối ưu hóa bóc tách PDF OCR bằng thuật toán hình thái học tổng quát (Zero-Hardcoded Vocabulary), bổ sung Tôn chỉ 1.7 & Quy chuẩn Clean Code 8.12 vào `AGENTS.md`, bảo đảm Zero-Impact trên DOCX/PDF Text.
- Test Suites:
  - Backend Full OCR & Knowledge: 75/75 passed (100%).
  - Ruff Linter: All checks passed (0 lỗi).
  - Frontend Biome: 171 files checked, 0 errors.
  - Frontend TypeScript: tsc --noEmit (0 lỗi).
  - Frontend Build: Vite build thành công.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/ocr/adapters/gemini_adapter.py` | [MODIFY] | Nâng cấp Prompt chuyên gia OCR (quy tắc bảng liên trang không chèn `---`, chống bẻ đôi từ qua trang, bảo toàn trọn vẹn ô mô tả 1 hàng = 1 bản ghi). Tích hợp gọi `post_process_ocr_output`. |
| `backend/app/modules/ocr/cleaner.py` | [NEW] | Module hậu xử lý độc lập cho OCR: Triệt tiêu hoàn toàn mảng từ khóa cứng `_COMMON_SPLIT_WORD_PAIRS`; triển khai thuật toán hình thái học và cấu trúc ô/cột tổng quát `repair_ocr_split_words_across_pages`; `stitch_ocr_multipage_tables`; `merge_ocr_orphan_table_rows`; `clean_ocr_table_syntax`; `post_process_ocr_output`. |
| `backend/app/modules/ocr/service.py` | [MODIFY] | Tích hợp `post_process_ocr_output` vào `_build_response` của `OCRService` giúp toàn bộ luồng OCR (Gemini, Mistral, Combo, Rescue) tự động được chuẩn hóa. |
| `backend/tests/test_ocr_cleaner.py` | [NEW] | 6 unit tests bao quát các tình huống bóc tách OCR phức tạp (ghép từ ranh giới trang không từ điển, hàn gắn bảng đa trang, gộp hàng mồ côi `<br>`, làm sạch cú pháp GFM). |
| `AGENTS.md` | [MODIFY] | Bổ sung Tôn chỉ 1.7 và Quy chuẩn Clean Code 8.12: Ưu tiên thuật toán tổng quát (Algorithmic-First), tuyệt đối cấm hardcode từ vựng/từ điển, và bắt buộc đề xuất phương pháp cho người dùng trước khi làm. |
| `Hợp đồng nâng cấp PM cổng thông tin điện tử_0001_boc_tach.md` | [MODIFY] | Chuẩn hóa toàn bộ tệp bóc tách hợp đồng thực tế từ 321 dòng rách vụn thành 1 Master Table hoàn chỉnh 74 hàng liên tục, 0 hàng mồ côi, 0 từ bị bẻ đôi, 100% GFM. |

## Kết Quả Kiểm Thử
- `uv run --extra dev pytest tests/test_ocr.py tests/test_ocr_cleaner.py tests/test_table_reconstructor.py tests/test_knowledge.py -v`: 75 passed in 30.75s (100%).
- `uv run ruff check .`: All checks passed (0 lỗi).
- `npm run lint`: Checked 171 files, 0 errors.
- `npm run typecheck`: 0 errors.
- `npm run build`: Vite build thành công.
