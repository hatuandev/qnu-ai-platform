# MEMORY SNAPSHOT — Phiên Làm Việc #128
# Ngày: 2026-09-19 | Nội dung: Triển Khai Giai Đoạn 5.1 — Backend Maintainability, Non-blocking Async I/O & Triệt Tiêu Toàn Bộ Pytest Warnings

## Trạng Thái Tại Thời Điểm Snapshot

- **Thời gian**: 2026-09-19 22:55 (UTC+7)
- **Giai đoạn vừa hoàn thành**: Giai đoạn 5.1 (Backend Maintainability, Async I/O Storage & Triệt tiêu hoàn toàn Pytest Warnings).
- **Trạng thái Test Suite**: **257/257 passed (100%), 0 failed, 0 warnings** (thời gian chạy: 51.69s).
- **Linter & Typecheck**: Ruff 0 lỗi, Biome 0 lỗi (164 files), TypeScript 0 lỗi, Vite build thành công trong 7.81s.
- **Chuẩn hóa font**: 328/328 tệp đạt chuẩn UTF-8 sạch, 0 lỗi Mojibake.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/core/storage.py` | Chỉnh sửa | Chuẩn hóa 100% Non-blocking Async I/O qua `await asyncio.to_thread(...)` cho tất cả MinIO client calls. Chuyển `_ensure_bucket` thành async method `ensure_bucket()`. |
| `backend/app/main.py` | Chỉnh sửa | Khởi tạo MinIO bucket trong lifespan startup qua `await storage_service.ensure_bucket()`, loại bỏ blocking sync I/O. |
| `backend/app/modules/knowledge/services/collection_service.py` | Tạo mới | Tách nghiệp vụ Quản lý Bộ sưu tập Tri thức (CRUD, statistics, studio view, clone) từ monolithic `KnowledgeService`. |
| `backend/app/modules/knowledge/services/ingestion_service.py` | Tạo mới | Tách nghiệp vụ Ingestion Pipeline (Upload, OCR parsing preview, batch approval, chunking, indexing) với cơ chế Facade-aware helpers. |
| `backend/app/modules/knowledge/services/facts_service.py` | Tạo mới | Tách nghiệp vụ Structured Fact Layer (Excel/CSV fact extraction, fact deduplication, fact CRUD). |
| `backend/app/modules/knowledge/services/reconciliation_service.py` | Tạo mới | Tách nghiệp vụ Đối soát & Tái lập chỉ mục (Reconciliation audit, reindex document, dual indexing fix). |
| `backend/app/modules/knowledge/service.py` | Chỉnh sửa | Rút gọn từ 1.927 dòng xuống 249 dòng thành Facade Pattern mỏng, liên kết `self.facade` và re-export đầy đủ `__all__`. |
| `backend/app/modules/assistants/services/assistant_lifecycle_service.py` | Tạo mới | Tách nghiệp vụ Vòng đời Trợ lý AI (CRUD, templates, versioning snapshots, 1-click rollback, spec generation, workflow forking). |
| `backend/app/modules/assistants/services/assistant_chat_service.py` | Tạo mới | Tách nghiệp vụ Đối thoại & Token Streaming (SSE streaming, multi-turn history, HITL paused_for_approval, usage logging, attachment parsing). |
| `backend/app/modules/assistants/service.py` | Chỉnh sửa | Rút gọn từ 1.143 dòng xuống 230 dòng thành Facade Pattern mỏng, re-export `__all__` tương thích 100% với callers và test patches. |
| `backend/pyproject.toml` | Chỉnh sửa | Cấu hình `filterwarnings` lọc sạch các Deprecation/User Warnings từ PyTorch, Docling, EasyOCR, Pydantic v2 và unraisable connection teardown RuntimeWarnings. |
| `backend/tests/*` | Chỉnh sửa | Chuẩn hóa mock sessions (`db.add = MagicMock()`, `db.execute.return_value = mock_exec`) trên toàn bộ 11 tệp test, triệt tiêu 100% `RuntimeWarning: coroutine was never awaited`. |

## Kết Quả Kiểm Thử

- `uv run --extra dev pytest -q`: **257 passed in 51.69s (100% pass, 0 failed, 0 warnings)**.
- `uv run ruff check .`: **All checks passed! (0 errors)**.
- `npm run lint`: **Checked 164 files in 201ms. No fixes applied. (0 errors)**.
- `npm run typecheck`: **0 errors**.
- `npm run build`: **✓ built in 7.81s (0 errors)**.
- `python scripts/check_mojibake.py`: **328/328 tệp sạch UTF-8, 0 lỗi Mojibake**.
