# MEMORY SNAPSHOT — Phiên Làm Việc #74
# Ngày: 2026-09-18 | Nội dung: Sửa lỗi OCR Bounding Box nát bét trên PDF — Loại bỏ khối giả lập, tích hợp SmartLayoutDetector

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-18 00:13 (UTC+7)
- Giai đoạn vừa hoàn thành: Sửa lỗi OCR Studio Verification — PDF bounding box synthetic stripes
- Test Suite: 143/143 passed (100%)

## Tóm Tắt Thay Đổi Trong Phiên Này

### Vấn đề
Khi upload file PDF (`4740 qd bgddt bo chi so cds dai hoc.pdf`) vào Kho Tri Thức và mở Document Verification Studio, khu nhận diện OCR (bounding box overlay) hiển thị sai hoàn toàn:
- Các khối chồng chéo ngang thành dải sọc đều nhau (84% chiều rộng, cách đều `step_y`)
- Badge hiển thị 40 ký tự markdown thô (`**bộ giáo dục và đào tạo**`, `# **quyết định**`)
- Trên trang ngang (bảng biểu landscape), các dải ngang cắt chéo qua bảng hoàn toàn lệch

### Nguyên nhân gốc rễ
1. `MistralOCRAdapter` (lines 113-140) tạo blocks giả lập: mỗi dòng markdown → 1 khối ngang `x=8.0, width=84.0, label=clean_line[:40]`
2. `_is_stale_raw_blocks` trong `KnowledgeService` chấp nhận khối giả (vì chứa type `title`/`header`) → không bao giờ kích hoạt `SmartLayoutDetector`
3. Frontend `document-bounding-visualizer.tsx` render `{box.label}` verbatim → badge dài 40 ký tự markdown

### Giải pháp

| Tệp | Hành Động | Mô Tả |
|---|---|---|
| `backend/app/modules/ocr/adapters/mistral_adapter.py` | Sửa đổi | Thay thế vòng lặp tạo blocks giả bằng `SmartLayoutDetector` thực (PyMuPDF hybrid + OpenCV). Hỗ trợ cả PDF và image. Trả `blocks = []` nếu detector thất bại |
| `backend/app/modules/knowledge/service.py` | Sửa đổi | Thêm phát hiện synthetic blocks trong `_is_stale_raw_blocks`: kiểm tra `x≈8.0, width≈84.0` hoặc markdown trong label (`**`, `#`). Nếu phát hiện → return `True` → tự động re-extract |
| `frontend/src/components/admin/document-bounding-visualizer.tsx` | Sửa đổi | Thêm `REGION_BADGE_LABELS` map (header→"tiêu đề đầu", table→"bảng biểu",...) và hàm `getDisplayBadge`. Badge có `max-width: 120px`, `text-overflow: ellipsis`, `pointer-events: none` |
| `backend/app/modules/workflows/router.py` | Auto-fix | Ruff sắp xếp imports alphabetically |
| `backend/app/modules/workflows/service.py` | Auto-fix | Ruff sắp xếp imports alphabetically |

### Xác minh trực tiếp trên `doc_d12f90421e2d`
- `_is_stale_raw_blocks` → `True` (phát hiện synthetic blocks cũ)
- Sau `get_studio_view`:
  - Page 1: 16 boxes (header, title, text, list) — bounding boxes chính xác
  - Page 4: 4 boxes — `table` (64.5% width, 79.5% height bao trùm bảng ngang), `signature`, `text`
- DB đã commit blocks mới

## Kết Quả Kiểm Thử
- uv run ruff check .: ✅ 0 lỗi (All checks passed!)
- uv run --extra dev pytest -v: ✅ 143/143 passed in 54.94s
- npm run lint: ✅ 0 lỗi (90 files checked)
- npm run typecheck: ✅ 0 lỗi
- npm run build: ✅ thành công trong 11.52s
