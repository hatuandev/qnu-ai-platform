# MEMORY SNAPSHOT — Phiên Làm Việc #28
# Ngày: 2026-09-16 | Nội dung: Kích Hoạt Chức Năng Chọn Tệp Tải Lên (FileUpload) & Pipeline Nạp Văn Bản Kho Tri Thức

---

## 1. Trạng Thái Tại Thời Điểm Snapshot

- **Thời gian**: 2026-09-16 16:35 (UTC+7)
- **Phiên số**: #28
- **Giai đoạn vừa hoàn thành**: Khắc phục dứt điểm vùng chọn tệp tĩnh trong Modal Nạp Văn Bản; Tích hợp component `FileUpload` có hỗ trợ File Picker của hệ điều hành, kéo thả tệp, tự động điền tiêu đề từ tên file; Thêm API `uploadDocument` và làm mới danh sách tài liệu con sau khi nạp.
- **Frontend Test Suite**: 
  - `npm run lint`: Biome 0 errors
  - `npm run typecheck`: TypeScript 0 errors
  - `npm run build`: Vite build thành công (1,083 kB)
  - Playwright Browser Subagent: Tải lên và kích hoạt pipeline nạp tài liệu thành công (100%)

---

## 2. Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp Tin | Hành Động | Mô Tả Kỹ Thuật |
| :--- | :---: | :--- |
| [`frontend/src/services/api-client.ts`](file:///d:/DuAnPhanMem/qnu-ai-platform/frontend/src/services/api-client.ts) | **Chỉnh sửa** | Bổ sung phương thức `uploadDocument(collectionId, file, title)` gửi `FormData` đến `POST /knowledge/collections/{id}/upload` và cập nhật dữ liệu tài liệu. |
| [`frontend/src/pages/collection-detail-page.tsx`](file:///d:/DuAnPhanMem/qnu-ai-platform/frontend/src/pages/collection-detail-page.tsx) | **Chỉnh sửa** | Thay thế dropzone tĩnh bằng component `FileUpload`, thêm state `selectedFile`, tự động điền tiêu đề từ tên tệp, kết nối `uploadDocument` và `invalidateQueries`. |
| [`frontend/src/pages/knowledge-page.tsx`](file:///d:/DuAnPhanMem/qnu-ai-platform/frontend/src/pages/knowledge-page.tsx) | **Chỉnh sửa** | Tương tự tích hợp component `FileUpload` vào Tab Ingest Wizard toàn cục. |

---

## 3. Kết Quả Kiểm Thử & Nghiệm Thu

1. **`npm run lint`**: 0 errors. Biome check 60 files pass hoàn toàn.
2. **`npm run typecheck`**: 0 errors. TypeScript compile không có lỗi type.
3. **`npm run build`**: Exit code 0. Vite production bundle đóng gói trong 8.46s.
4. **Auto-Testing Browser Playwright**:
   - Mở modal Nạp Văn Bản Mới.
   - Component `FileUpload` hiển thị dòng chữ *"Kéo thả tệp tài liệu vào đây hoặc chọn từ máy tính"*.
   - Nhập tiêu đề hoặc chọn tệp từ máy tính.
   - Bấm "Khởi Động Pipeline Nạp" thực thi thành công.
   - Danh mục tài liệu trong bộ sưu tập tăng từ 1 lên 2 tệp, hiển thị tài liệu mới với 4 Chunks và trạng thái Sẵn sàng.
   - Video ghi hình: `verify_file_upload_1789551038455.webp`.
