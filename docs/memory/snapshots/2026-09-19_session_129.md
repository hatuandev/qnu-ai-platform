# MEMORY SNAPSHOT — Phiên Làm Việc #129
# Ngày: 2026-09-19 | Nội dung: Triển Khai Giai Đoạn 5.2 — ModelOps Decomposition, Observability & Production Docker Baseline

## Trạng Thái Tại Thời Điểm Snapshot

- **Thời gian**: 2026-09-19 23:05 (UTC+7)
- **Giai đoạn vừa hoàn thành**: Giai đoạn 5.2 (ModelOps Decomposition, In-Memory Metrics & Prometheus Exporter, Docker Compose Production Baseline).
- **Trạng thái Test Suite**: **257/257 passed (100%), 0 failed, 0 warnings** (thời gian chạy: 49.49s).
- **Linter & Typecheck**: Ruff 0 lỗi, Biome 0 lỗi (164 files), TypeScript 0 lỗi, Vite build thành công trong 7.24s.
- **Chuẩn hóa font**: 334/334 tệp đạt chuẩn UTF-8 sạch, 0 lỗi Mojibake.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/modelops/services/provider_service.py` | Tạo mới | Tách nghiệp vụ Quản lý Provider, API Key pool, Fernet encryption, key rotation, ping/health check từ monolithic `ModelOpsService`. |
| `backend/app/modules/modelops/services/model_catalog_service.py` | Tạo mới | Tách nghiệp vụ Quản lý System Model Defaults (`embedding`, `reranker`, `ocr`) và dynamic model options discovery. |
| `backend/app/modules/modelops/services/usage_accounting_service.py` | Tạo mới | Tách nghiệp vụ Quản lý Token Quota (`check_quota_available`), ghi nhận `LLMUsageLog`, và thống kê chi phí/token. |
| `backend/app/modules/modelops/services/inference_service.py` | Tạo mới | Tách nghiệp vụ Thực thi LLM Inference (`generate`, `generate_stream`), Dynamic Fallback routing, và Circuit Breaker. Hỗ trợ `_get_adapter_factory()` tương thích 100% với monkeypatching trong unit tests. |
| `backend/app/modules/modelops/service.py` | Chỉnh sửa | Rút gọn từ 2.365 dòng xuống ~270 dòng thành Facade Pattern mỏng, liên kết `self.facade` và re-export đầy đủ `__all__`. |
| `backend/app/core/observability.py` | Tạo mới | Thread-safe `MetricsRegistry` thu thập uptime, request count, latency trung bình, status code breakdown và xuất chuẩn Prometheus exposition text format. |
| `backend/app/core/middleware.py` | Chỉnh sửa | Tích hợp `metrics_registry.record_request()` vào `RequestTimingMiddleware`. |
| `backend/app/main.py` | Chỉnh sửa | Thêm endpoint `/metrics` hỗ trợ cả Prometheus text format lẫn JSON format (`?format=json`). |
| `docker-compose.yml` | Chỉnh sửa | Thêm service `backend` với profiles `["app", "full"]`, liên kết dependency `condition: service_healthy` với `postgres`, `redis`, `qdrant`, `minio`. |

## Kết Quả Kiểm Thử

- `uv run --extra dev pytest -q`: **257 passed in 49.49s (100% pass, 0 failed, 0 warnings)**.
- `uv run ruff check .`: **All checks passed! (0 errors)**.
- `npm run lint`: **Checked 164 files in 157ms. No fixes applied. (0 errors)**.
- `npm run typecheck`: **0 errors**.
- `npm run build`: **✓ built in 7.24s (0 errors)**.
- `python scripts/check_mojibake.py`: **334/334 tệp sạch UTF-8, 0 lỗi Mojibake**.
