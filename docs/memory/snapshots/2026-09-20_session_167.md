# MEMORY SNAPSHOT — Phiên Làm Việc #167
# Ngày: 2026-09-20 | Nội dung: Tinh giản giao diện Trợ lý AI & Tách DAG Visual Studio thành màn hình độc lập

## Trạng Thái Tại Thời Điểm Snapshot

- Thời gian: 2026-09-20 23:25 (UTC+7)
- Phiên số: #167
- Giai đoạn vừa hoàn thành:
  - Loại bỏ các thông tin dư thừa, dữ liệu giả và giảm tải độ rối mắt trên màn hình Chi tiết Trợ lý (`/assistants/:id`):
    - Xóa 4 thẻ KPI thô/giả (128 lượt hội thoại, độ trễ 240ms, thẻ kho tri thức trùng lặp, thẻ Ragas TM-08 trùng lặp).
    - Xóa chuỗi ID kỹ thuật dài dòng (`ast_admissions`) ở tiêu đề.
    - Làm dịu dải thông báo Publish Gate 5 tiêu chí với nền phẳng tinh tế, không còn nhuộm đỏ chói toàn màn hình.
    - Xóa hộp văn bản lý thuyết tĩnh về chiến lược Chunking trong phần Gắn kết Tri thức RAG.
  - Tách biệt hoàn toàn đồ thị DAG Studio thành màn hình Studio toàn diện độc lập (`/workflows/:id`):
    - Thêm nút hành động trực tiếp `[Sơ đồ DAG Studio ↗]` tại Header và phần Cấu hình Quy trình, gắn kèm tham số `returnTo=/assistants/:code`.
    - Cập nhật trang `dag-canvas-page.tsx` hỗ trợ nút quay lại thông minh `[Quay lại Trợ lý]` để quay về đúng Trợ lý đang biên soạn.
    - Tinh gọn thanh Workspace Nav chỉ còn **3 tabs cấu hình trọng tâm**: `overview` (Thông tin & Tri thức), `models` (Mô hình & An toàn), `tools` (Quy trình & Công cụ).
    - Loại bỏ việc nhúng lồng `DAGCanvasPage` bên trong subtab để tránh tình trạng "Monolithic Tabbed Page" (tuân thủ nguyên tắc 4.5 AGENTS.md), giảm tải bundle của trang Chi tiết Trợ lý.
  - Tăng cường khả năng phục hồi (Resilience) Backend: dùng `getattr` truy xuất an toàn `doc.status` trong `ingestion_service.py`.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
|---|---|---|
| `frontend/src/components/assistants/assistant-header.tsx` | Sửa | Xóa 4 thẻ KPI thô; xóa ID kỹ thuật `assistant.id`; thêm nút `[Sơ đồ DAG Studio]`; làm dịu container Publish Gate. |
| `frontend/src/components/assistants/assistant-workspace-nav.tsx` | Sửa | Rút gọn `WORKSPACE_TABS` từ 8 tabs cồng kềnh xuống còn 3 tabs cấu hình trọng tâm (`overview`, `models`, `tools`). |
| `frontend/src/components/assistants/sections/assistant-knowledge-section.tsx` | Sửa | Xóa bỏ hộp khuyến nghị chunking lý thuyết tĩnh, xóa import `Sparkles` không dùng. |
| `frontend/src/components/assistants/sections/assistant-tools-section.tsx` | Sửa | Thêm tham số `returnTo` vào nút mở đồ thị DAG Studio. |
| `frontend/src/pages/assistant-detail-page.tsx` | Sửa | Tự động chuyển hướng subview workflow sang `/workflows/:id?returnTo=...`; bỏ component lồng `DAGCanvasPage`; xóa `runsQuery` & `kpiStats` thừa. |
| `frontend/src/pages/dag-canvas-page.tsx` | Sửa | Đọc tham số `returnTo`, cập nhật nút Back và thêm nút `[Quay lại Trợ lý]` cho luồng chuyển trang mượt mà. |
| `backend/app/modules/knowledge/services/ingestion_service.py` | Sửa | Dùng `getattr` truy xuất an toàn `doc.status` và `doc.index_status` trong `get_studio_view`. |

## Kết Quả Kiểm Thử
- `npm run typecheck`: Pass (0 lỗi, `tsc --noEmit`)
- `npm run lint`: Pass (0 lỗi, Biome 167 files sạch)
- `npm run build`: Pass (Vite production build thành công trong 13.54s)
- `uv run ruff check .`: Pass (All checks passed)
- Backend Pytest: 329/329 passed
