# MEMORY SNAPSHOT — Phiên Làm Việc #174
# Ngày: 2026-09-21 | Nội dung: Bổ Sung Node Query Rewrite Vào Seed Data Của Cả 5 Mô Đun Quy Trình DAG

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-21 14:32 (UTC+7)
- **Giai đoạn vừa hoàn thành**:
  * Tích hợp node `query_rewrite` với prompt chuẩn hóa chuyên biệt vào seed data của cả 5 quy trình DAG chính thức (`admissions-assistant`, `regulations-assistant`, `library-assistant`, `drafting-assistant`, `question-bank-assistant`).
  * Nâng cấp phương thức `sync_default_workflows` trong `service.py` để tự động cập nhật bản nháp (`WorkflowDraft`), phiên bản v1.0.0 (`WorkflowVersion`) và định nghĩa (`WorkflowDefinition`) khi tệp JSON thay đổi.
  * Đồng bộ thành công 5/5 quy trình vào CSDL PostgreSQL qua lệnh CLI `uv run python -m app.cli db seed --workflows`.
  * Cập nhật tài liệu quy trình hệ thống [`docs/quy_trinh/04_dieu_phoi_tro_ly_dag.md`](../../quy_trinh/04_dieu_phoi_tro_ly_dag.md) bổ sung Mục 13 về kiến trúc node dùng chung và bảng cấu hình 5 quy trình.
- **Trạng thái Test Suite**:
  * Backend Ruff: 0 lỗi linter
  * Backend Pytest: 31/31 passed (100%)
  * Frontend Biome: 168 files checked, 0 lỗi
  * Frontend TypeScript: 0 lỗi
  * Database Seed Parity: 5/5 workflows có node `query.rewrite` kèm instruction đầy đủ trong CSDL

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `configs/workflows/regulations-assistant.v1alpha1.json` | Chỉnh sửa | Bổ sung node `query_rewrite` chuyên quy chế tín chỉ, ĐRL, GPA, CTĐT; nối `chat_input` -> `query_rewrite` -> `knowledge_answer` |
| `configs/workflows/library-assistant.v1alpha1.json` | Chỉnh sửa | Bổ sung node `query_rewrite` chuyên giáo trình, tài liệu, luận văn; nối `chat_input` -> `query_rewrite` -> `knowledge_answer` |
| `configs/workflows/drafting-assistant.v1alpha1.json` | Chỉnh sửa | Bổ sung node `query_rewrite` chuyên soạn thảo văn bản hành chính NĐ 30; nối `chat_input` -> `query_rewrite` -> `extract_fields` & `sample_retrieval` |
| `configs/workflows/question-bank-assistant.v1alpha1.json` | Chỉnh sửa | Bổ sung node `query_rewrite` chuyên khảo thí, ngân hàng đề thi, thang đo Bloom, CLO/PLO; nối `chat_input` -> `query_rewrite` -> `knowledge_answer` |
| `backend/app/modules/workflows/service.py` | Chỉnh sửa | Cập nhật `sync_default_workflows` tự động cập nhật bản nháp và phiên bản v1.0.0 khi cấu hình JSON workflow thay đổi |
| `docs/quy_trinh/04_dieu_phoi_tro_ly_dag.md` | Chỉnh sửa | Thêm Mục 13 chi tiết hóa kiến trúc node dùng chung và bảng đặc tả cấu hình 5 mô đun quy trình |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi linter
- `uv run --extra dev pytest tests/test_workflows.py tests/test_query_rewrite_node.py -v`: **31/31 passed (100%)**
- `npm run lint`: Biome check 168 files, **0 lỗi**
- `npm run typecheck`: TypeScript tsc --noEmit, **0 lỗi**
- CSDL Inspection: 5/5 workflows xác nhận có `query.rewrite` hoạt động chính xác.
