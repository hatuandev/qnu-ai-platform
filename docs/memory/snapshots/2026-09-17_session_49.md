# MEMORY SNAPSHOT — Phiên Làm Việc #49
# Ngày: 2026-09-17 | Nội dung: Sửa Lỗi Xóa Tài Liệu Thất Bại HTTP 404 (Loại Bỏ Mock Trap Trong getDocuments)

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 23:59 (UTC+7)
- Giai đoạn vừa hoàn thành: Triệt tiêu Mock Trap trong API Client (trả về danh sách thật khi backend trả về mảng rỗng `[]`), Idempotent Delete trên 404
- Test Suite Backend: 117/117 pytest passed (100%), Ruff check 0 lỗi
- Test Suite Frontend: Biome lint 0 lỗi trên 77 files, TypeScript 0 lỗi, Vite build thành công (5.36s)

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `frontend/src/services/api-client.ts` | MODIFY | Thay thế `Array.isArray(data) && data.length > 0` bằng `Array.isArray(data)` trong `getDocuments` và `getCollections` để không tự ý fallback sang mock `doc_ts_2026` khi kho rỗng; cập nhật `deleteDocument` coi 404 là idempotent success. |
| `docs/memory/PROJECT_CONTEXT.md` | MODIFY | Cập nhật snapshot phiên #49, trạng thái kiểm thử và giải pháp. |

## Kết Quả Kiểm Thử
- `npm run lint`: Biome check 77 files, 0 lỗi.
- `npm run typecheck`: TypeScript tsc --noEmit 0 lỗi.
- `npm run build`: Vite build hoàn tất thành công trong 5.36s.
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest tests/test_knowledge.py -v`: 23/23 tests passed.
- `uv run --extra dev pytest -q`: 117/117 tests passed (100%).
