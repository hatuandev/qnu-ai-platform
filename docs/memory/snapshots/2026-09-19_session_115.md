# MEMORY SNAPSHOT — Phiên Làm Việc #115
# Ngày: 2026-09-19 | Nội dung: Triển Khai Giai Đoạn 5: Chuẩn Hóa Vòng Đời Tri Thức, Indexing Nguyên Tử & Đối Soát Bền Vững 4 Tầng (P1-09 đến P1-13)

---

## 1. Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-19 17:45 (UTC+7)
- **Giai đoạn vừa hoàn thành**: Giai đoạn 5 (P1-09 đến P1-13) — Chuẩn Hóa Vòng Đời Tri Thức, Indexing Nguyên Tử & Đối Soát Bền Vững 4 Tầng.
- **Test Suite**:
  - Backend: 232/232 Pytest passed (100%), Ruff 0 lỗi.
  - Frontend: Biome check 130 files 0 lỗi, TypeScript 0 lỗi, Vite build thành công trong 7.59s.
  - Zero Mojibake: 286/286 files sạch 100%.

---

## 2. Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/models.py` | Cập nhật | Bổ sung `index_status` (`pending`, `indexing`, `indexed`, `index_failed`) và `index_error` vào `KnowledgeDocument`. |
| `backend/app/modules/knowledge/schemas.py` | Cập nhật | Bổ sung `index_status`, `index_error` vào DTOs; thêm schemas `ReindexDocumentResponse`, `KnowledgeReconciliationResponse`, `KnowledgeReconciliationDiscrepancy`, `ReconcileFixResponse`. |
| `backend/app/modules/knowledge/service.py` | Cập nhật | Tách `job_type="ingestion_extract"` và `"vector_indexing"`; cập nhật `approve_document` bắt lỗi indexing và ghi `index_status="index_failed"` thay vì nuốt lỗi; thêm `reindex_document`; dọn sạch file vật lý trong `delete_collection`; thêm `reconcile_collection` đối soát 4 tầng và `reconcile_fix_collection` tự động sửa lỗi. |
| `backend/app/modules/knowledge/router.py` | Cập nhật | Thêm 3 endpoints: `POST /documents/{id}/reindex`, `GET /collections/{id}/reconcile`, `POST /collections/{id}/reconcile-fix`. |
| `backend/tests/test_knowledge.py` | Cập nhật | Thêm 4 tests mới cho `index_status`, `index_failed`, `reindex` và `reconcile`. 31/31 tests passed. |
| `frontend/src/types/knowledge.ts` | Cập nhật | Bổ sung `index_status`, `index_error` vào `KnowledgeDocument`; thêm interfaces đối soát và reindex. |
| `frontend/src/services/knowledge-api.ts` | Cập nhật | Bổ sung mapping `index_status`, `index_error`; thêm API methods `reindexDocument`, `reconcileCollection`, `fixReconciliation`. |
| `frontend/src/pages/collection-detail-page.tsx` | Cập nhật | Thêm nút `[Đối soát Kho]`, huy hiệu kép trạng thái, nút `[⚡ Thử lại Index]`, Dialog đối soát 4 tầng với 4 KPI cards và nút `[Đồng bộ tất cả]`. |
| `docs/quy_trinh/02_nap_tri_thuc_minio.md` | Cập nhật | Bổ sung Bước 11 về vòng đời lập chỉ mục kép, tách hàng đợi và đối soát 4 tầng. |
| `docs/nhat_ky/2026-09-19_phien_115_trien_khai_giai_doan_5_vong_doi_tri_thuc_indexing_reconciliation.md` | Tạo mới | Nhật ký phiên 115 chi tiết. |
| `docs/WORK_LOG.md` | Cập nhật | Bổ sung phiên 115 vào bảng tổng hợp. |
| `docs/memory/PROJECT_CONTEXT.md` | Cập nhật | Cập nhật phiên mới nhất #115 và tóm tắt kết quả Giai đoạn 5. |

---

## 3. Kết Quả Kiểm Thử
- **npm run lint**: Checked 130 files in 150ms. 0 error.
- **npm run typecheck**: tsc --noEmit: 0 error.
- **npm run build**: Vite production bundle built in 7.59s.
- **uv run ruff check .**: All checks passed!
- **uv run --extra dev pytest -v**: 232 passed, 40 warnings in 82.79s (100% passed).
- **python scripts/check_mojibake.py**: 286/286 files 100% clean UTF-8.
