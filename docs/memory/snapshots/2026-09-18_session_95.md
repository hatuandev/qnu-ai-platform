# MEMORY SNAPSHOT — Phiên Làm Việc #95
# Ngày: 2026-09-18 | Nội dung: Hiệu chỉnh nhận xét và review code worktree

## Trạng Thái Tại Thời Điểm Snapshot

- Thời gian: 2026-09-18 20:30 (UTC+7)
- Giai đoạn vừa hoàn thành: Cập nhật báo cáo sau cải thiện theo quyết định chỉ dùng Dev Access Gate đơn giản; review độc lập phần code hiện có trong worktree.
- Maturity: giữ ở **6,4/10 — Internal Beta**. Đợt thay đổi đang review chưa đủ điều kiện bàn giao.
- Phạm vi truy cập phù hợp hiện tại: password hash phía backend + signed `HttpOnly` session + principal cố định; chưa cần RBAC/SSO đa vai trò.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `docs/nhan_xet_sau_cai_thien_platform_2026-09-18.md` | Cập nhật | Bổ sung review mã, đánh giá provenance seed, evaluation, lifecycle RAG, workflow/tool policy và hướng Dev Access Gate. |
| `docs/memory/PROJECT_CONTEXT.md` | Cập nhật | Ghi nhận phiên #95, quality blockers và backlog Dev Access Gate. |
| `docs/memory/snapshots/2026-09-18_session_95.md` | Tạo | Lưu snapshot trạng thái review. |
| `docs/nhat_ky/2026-09-18_phien_95_review_code_va_hieu_chinh_nhan_xet.md` | Tạo | Nhật ký chi tiết của phiên. |
| `docs/WORK_LOG.md` | Cập nhật | Thêm mục lục phiên #95. |

## Phát Hiện Review Quan Trọng

1. Library/Question Bank seed được gọi là official nhưng không có file gốc, provenance hay evidence per fact; Core chỉ cung cấp template/benchmark tổng quát, không là nguồn xác thực cho các fact QNU cụ thể.
2. `KnowledgeService` có các method cleanup bị định nghĩa trùng. Trong lúc review, diff song song thêm một `delete_document` nữa; bản cuối che bản cũ và làm mất bước xóa storage.
3. Seeder Assistant ghi đè prompt, collection và config của Assistant tồn tại khi startup.
4. Evaluation vẫn dựng answer/context từ ground truth nếu runtime thiếu kết quả; TM-08 chưa là quality gate độc lập.
5. Qdrant dense filter chỉ dùng `is_active`; chưa bind lifecycle, tenant, workspace, document revision.
6. Chat vẫn bypass approval vì tin client flag; Tool Gateway chưa enforce Assistant allowlist/approval. Workflow version có cải thiện trong diff mới nhưng vẫn fail-open nếu version record bị thiếu.
7. Question Bank mới là workflow tra cứu có citation, không còn workflow tạo câu hỏi/ma trận/rubric có approval. Cần tách hai use case.

## Kết Quả Kiểm Thử

- `uv run ruff check .`: **Fail — 7 lỗi tại snapshot đầu review**.
  - 6 lỗi liên quan worktree: duplicate stopword, 3 redefinition và 2 undefined type annotation.
  - 1 duplicate stopword ngoài phần sửa trong `openai_adapter.py`.
- `uv run --extra dev pytest tests/test_assistants.py tests/test_evaluation.py tests/test_rag.py tests/test_knowledge.py tests/test_workflows.py -v`: **71 passed, 1 failed, 17 warnings tại snapshot đầu review**.
  - Failure: `test_seed_preserves_existing_user_configuration`.
- `python scripts/check_mojibake.py`: **267 files scanned, no errors**.
- Frontend: không có thay đổi frontend trong worktree reviewed; không chạy lại lint/typecheck/build frontend.

## Ghi Chú Phối Hợp

- Không sửa các file backend/config seed đang có thay đổi để tránh ghi đè session seed data song song của người dùng. Session song song tiếp tục đổi code khi review diễn ra; cần rerun quality gate khi session đó kết thúc.
- Không cập nhật `docs/quy_trinh/`: phiên này chỉ review/tài liệu, không thay đổi luồng nghiệp vụ hay runtime.
