# MEMORY SNAPSHOT — Phiên Làm Việc #68
# Ngày: 2026-09-17 | Nội dung: Tự Động Hóa 100% Loại Văn Bản (Taxonomy) Cho Cả Chọn Tệp & Kéo Thả

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 22:33:00 (GMT+7)
- Giai đoạn vừa hoàn thành: Nâng cấp toàn diện cơ chế Auto-detection cho Loại văn bản (Document Type) trong Kho Tri Thức:
  1. Hỗ trợ đối chiếu từ khóa tên tệp thông minh (30+ mẫu regex tiếng Việt, bao gồm tuyển sinh, đào tạo, đề án, quy chế,...).
  2. Hỗ trợ tự động thiết lập mặc định theo ngữ cảnh Kho tri thức (`collectionContext`: id, name, code) ngay khi vào trang hoặc khi tên tệp không có từ khóa đặc thù.
  3. Đồng bộ bộ tính điểm ưu tiên pháp lý `getPriorityForDocumentType` chuẩn theo 37 loại văn bản NĐ 30 / QNU Catalog (Cốt lõi 10/10, Tiêu chuẩn 8/10, Tham khảo 6/10).
  4. Hỗ trợ nhận diện tự động trên cả 2 luồng: Click duyệt chọn tệp (`<input type="file">`) và Kéo thả tệp trực tiếp (`onDrop` / `onDragOver`).
  5. Cập nhật nhãn loại văn bản trong Smart Recommendation Banner hiển thị tiếng Việt sắc nét và tức thì.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `frontend/src/lib/file-inspector.ts` | MODIFY | Mở rộng `detectDocumentTypeFromFilename` chấp nhận `collectionContext` đa hình (id, name, code); mở rộng `getPriorityForDocumentType` đồng bộ với catalog 37 loại (`de_an` có priority 8/10). |
| `frontend/src/pages/document-ingest-page.tsx` | MODIFY | Tách hàm `processFile` dùng chung cho cả `onChange` và `onDrop`; bổ sung `useEffect` khởi tạo Loại văn bản mặc định theo Kho tri thức; làm đẹp hiển thị Loại văn bản trong Banner đề xuất. |
| `docs/memory/snapshots/2026-09-17_session_68.md` | NEW | Snapshot phiên làm việc #68. |

## Kết Quả Kiểm Thử
- `npm run lint`: Biome check 86 files — **0 lỗi**.
- `npm run typecheck`: TypeScript tsc --noEmit — **0 lỗi**.
- `npm run build`: Vite build thành công trong 10.81s.
- `uv run ruff check .`: **All checks passed! (0 lỗi)**.
- `uv run --extra dev pytest tests/test_knowledge.py`: **25/25 tests passed (100%)**.
