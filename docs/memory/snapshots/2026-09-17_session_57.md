# MEMORY SNAPSHOT — Phiên Làm Việc #57
# Ngày: 2026-09-17 | Nội dung: Sửa Lỗi Hiển Thị Markdown Dồn Trang 1 & Trang 2 Trắng Tinh Trong Document Verification Studio

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-17 19:58 (UTC+7)
- Phiên số: #57
- Giai đoạn: Document Verification Studio & Knowledge Ingestion Engine

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/service.py` | MODIFIED | Bóc tách Markdown và bảng biểu độc lập cho từng trang trong `_ensure_page_blocks`, lưu `page_markdowns` vào metadata. Cập nhật `build_studio_pages` để không bao giờ dồn toàn bộ chunks vào Trang 1, ưu tiên `page_markdowns` và tổng hợp Markdown chuẩn từ blocks của từng trang. |
| `backend/tests/test_knowledge.py` | MODIFIED | Bổ sung 2 unit test `test_build_studio_pages_per_page_markdown_and_blocks` và `test_build_studio_pages_synthesizes_from_blocks_when_chunks_clumped` kiểm chứng phân trang Markdown độc lập và chống trang trắng. |
| `frontend/vite.config.ts` | MODIFIED | Đổi target proxy từ `http://localhost:8001` sang `http://127.0.0.1:8001` chống lỗi phân giải IPv6 `::1` trên Windows. |
| `make.bat` & `make.ps1` | MODIFIED | Thêm độ trễ 2s giữa Backend và Frontend trong `make dev` để tránh race condition khi khởi động. |

## Kết Quả Kiểm Thử
- Backend Ruff Check: 0 lỗi (`uv run ruff check .` All checks passed!)
- Backend Pytest: 3/3 tests passed (`test_build_studio_pages_*` 100% pass)
- Frontend Typecheck: 0 lỗi (`npm run typecheck` exit 0)
