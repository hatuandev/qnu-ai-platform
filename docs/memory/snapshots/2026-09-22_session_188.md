# MEMORY SNAPSHOT — Phiên Làm Việc #188
# Ngày: 2026-09-22 | Nội dung: Khắc phục triệt để lỗi rò rỉ dữ liệu bảng thô & Nâng cấp chất lượng trình bày RAG toàn diện

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-22
- Giai đoạn vừa hoàn thành: Khắc phục triệt để lỗi sao chép bảng thô (`||||||`), rò rỉ các ngành không liên quan (over-inclusion), vỡ cú pháp bảng, và tích hợp cơ chế 4 tầng (Entity-Scoped Extraction, Dynamic Formatting, Pedagogical Prompts, Post-processing Sanitizer).
- Test Suite:
  * Backend: `pytest tests/test_rag_entity_scoped_formatting.py -v` (6/6 passed), `pytest tests/test_rag.py -v` (26/26 passed), `ruff check .` (0 lỗi).
  * Frontend: `npm run lint` (0 lỗi), `npm run typecheck` (0 lỗi), `npm run build` (thành công 100%).

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/rag/composer.py` | MODIFY | Bổ sung `sanitize_rag_answer()` làm sạch pipe thô & phục hồi dòng thực thể; thêm `get_format_instructions()` cho `AnswerFormatPlanner`; cập nhật `SYSTEM_PROMPT_TEMPLATE`. |
| `backend/app/modules/rag/query_router.py` | MODIFY | Thêm `target_entities` vào `QueryAnalysis`, tự động phát hiện mã ngành, tên ngành chuẩn và thực thể được người dùng hỏi. |
| `backend/app/modules/rag/service.py` | MODIFY | Tự động inject Ràng buộc trích xuất theo thực thể (`target_entities`), inject chỉ đạo định dạng của `AnswerFormatPlanner` vào prompt LLM; gọi `sanitize_rag_answer()` ở tầng hậu xử lý; cập nhật `build_generic_system_instruction()`. |
| `backend/app/modules/assistants/seeder.py` | MODIFY | Cập nhật `system_prompt` của `ast_admissions` với quy tắc Entity-Scoped và giải thích phương thức xét tuyển rõ ràng. |
| `configs/workflows/admissions-assistant.v1alpha1.json` | MODIFY | Cập nhật node `knowledge_answer` với chỉ thị trích xuất theo thực thể và cấm sao chép rác bảng. |
| `backend/scripts/sync_assistant_prompts.py` | NEW | Script đồng bộ prompt cập nhật vào CSDL PostgreSQL. |
| `backend/tests/test_rag_entity_scoped_formatting.py` | NEW | Suite kiểm thử tự động 6 ca kiểm tra làm sạch pipe, lọc theo ngành, bảng markdown, định dạng tổ hợp môn và prompt assembly. |
| `docs/quy_trinh/03_hybrid_rag_truy_xuat.md` | MODIFY | Bổ sung Mục 12 tài liệu quy trình về Ràng buộc thực thể, Chỉ đạo định dạng và Bộ lọc hậu xử lý. |

## Kết Quả Kiểm Thử
- `npm run lint`: 0 lỗi.
- `npm run typecheck`: 0 lỗi.
- `npm run build`: Thành công.
- `uv run ruff check .`: 0 lỗi.
- `uv run --extra dev pytest tests/test_rag_entity_scoped_formatting.py -v`: 6 passed (100%).
- `uv run --extra dev pytest tests/test_rag.py -v`: 26 passed (100%).
- Chat thực nghiệm:
  * Câu hỏi: *"bạn biết ngành công nghệ thông tin cần những môn học nào để xét tuyển không ?"*
  * Kết quả: Trả lời đúng duy nhất ngành CNTT (mã 7480201), danh sách gạch đầu dòng 5 tổ hợp môn sạch sẽ, giải thích rõ các phương thức 1, 2, 3, 4; 0 ký tự `||||||` rác, 0 dòng ngành Luật/Hóa học.
  * Câu hỏi: *"xin chào cho tôi biết các phương thức tuyển sinh năm 2026 của trường"*
  * Kết quả: Bảng Markdown 5 phương thức chuẩn chỉnh, không thừa học phí.
