# MEMORY SNAPSHOT — Phiên Làm Việc #75
# Ngày: 2026-09-18 | Nội dung: Sửa triệt để lỗi chuẩn hóa Markdown bị mất bảng biểu thành 'Bảng biểu dữ liệu số hóa' và nâng cấp DocxParser theo qnu-ai-core

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-18 00:45 (UTC+7)
- Giai đoạn vừa hoàn thành: Nâng cấp DocxParser tuần tự, Layout Detector Table Extraction & Markdown Table Cleaning
- Test Suite: 100% Passed (Pytest + Ruff + Biome + TSC + Vite)

## Tóm Tắt Thay Đổi Trong Phiên Này

### Vấn đề
Khi người dùng tải tệp (nhất là DOCX tuyển sinh 14 trang `Thong tin tuyen sinh dai hoc 2026_Lan2-1 (1).docx`) lên Kho Tri Thức:
1. Toàn bộ các bảng biểu biểu mẫu tuyển sinh từ trang 2 đến trang 13 bị biến thành chuỗi chữ giữ chỗ: `Bảng biểu dữ liệu số hóa` thay vì trích xuất thành bảng Markdown (`| STT | Mã xét tuyển | ... |`).
2. Khung scan bên trái nhận diện được vùng `[bảng biểu]`, nhưng tab Markdown và nút tải xuống bị rỗng bảng hoặc chỉ có dòng chữ giữ chỗ.

### Nguyên nhân gốc rễ
1. `backend/app/modules/ocr/layout_detector.py` (dòng 295, 405) gán cứng `"text": "Bảng biểu dữ liệu số hóa"`, không gọi `tab.extract()`, đồng thời loại bỏ toàn bộ text con trong bảng.
2. `DocxParser` trong `office_parser.py` bóc tách tách rời đoạn văn trước, dồn toàn bộ bảng xuống cuối tệp, và chỉ trả về `page_count = 1` khiến chunks bị dồn về trang 1 (`is_clumped = True`).
3. Trong `service.py`, `_synthesize_page_markdown_from_blocks` lấy text từ block của trang mà block table lại chứa chuỗi giữ chỗ nói trên, dẫn đến toàn bộ nội dung từ trang 2 đến trang 13 biến thành `"Bảng biểu dữ liệu số hóa"`.

### Giải pháp kỹ thuật

| Tệp | Hành Động | Mô Tả |
|---|---|---|
| `backend/app/modules/ocr/layout_detector.py` | Sửa đổi | Bổ sung `_format_table_markdown(rows)`. Khi `find_tables()` phát hiện bảng, gọi `tab.extract()` để chuyển đổi thành Markdown table thực sự. Gán vào `text` và `content_snippet`. Triệt tiêu hoàn toàn chuỗi placeholder. |
| `backend/app/modules/knowledge/parsers/office_parser.py` | Sửa đổi | Nâng cấp toàn diện `DocxParser` theo chuẩn `docx_processor.py` của `qnu-ai-core`: duyệt tuần tự `child in doc.element.body`, chuẩn hóa Quốc hiệu/Tiêu ngữ/Số hiệu NĐ 30, tách hàng La Mã, xử lý colspan và ngắt dòng trong ô `<br>`/`;`. |
| `backend/app/modules/knowledge/cleaner.py` | Sửa đổi | Bổ sung Unicode NFC (Zero Mojibake), chuẩn hóa cấu trúc bảng Markdown (`_normalize_markdown_table_block`), nối bảng qua trang (`_stitch_table_continuations`), làm sạch số trang đơn độc. |
| `backend/app/modules/knowledge/service.py` | Sửa đổi | Cập nhật `_synthesize_page_markdown_from_blocks` giữ nguyên bảng Markdown, bổ sung phát hiện `has_placeholder` trong `_is_stale_raw_blocks` để tự động re-extract dữ liệu cũ, lưu `text` đầy đủ cho từng block trong `_ensure_page_blocks`. |
| `backend/app/modules/knowledge/parsers/blocks.py` | Sửa đổi | Bổ sung `_format_table_markdown` và cập nhật `extract_page_blocks` trích xuất bảng Markdown từ PDF vector tables. |
| `frontend/src/services/verification-data.ts` | Sửa đổi | Dọn dẹp sạch 12 vị trí chứa chuỗi `"Bảng biểu dữ liệu số hóa"` trong mock fixture. |
| `backend/tests/test_knowledge_docx_tables.py` | Tạo mới | Unit tests kiểm thử bóc tách tuần tự DOCX, format table markdown và cleaner. |

### Kết Quả Kiểm Thử
- **Parse thực tế `Thong tin tuyen sinh dai hoc 2026_Lan2-1 (1).docx`**: 23.118 ký tự, 4 bảng lớn, đầy đủ 53 ngành tuyển sinh và tổ hợp môn, 0 chuỗi placeholder.
- `uv run ruff check .`: ✅ 0 lỗi (All checks passed!)
- `uv run --extra dev pytest tests/test_knowledge_docx_tables.py tests/test_smart_layout.py -v`: ✅ 7/7 passed in 2.98s
- `uv run --extra dev pytest tests/test_knowledge.py -v`: ✅ 25/25 passed in 58.98s
- `npm run lint`: ✅ 0 lỗi (91 files checked)
- `npm run typecheck`: ✅ 0 lỗi
- `npm run build`: ✅ thành công trong 17.15s
- `python scripts/check_mojibake.py`: ✅ 225/225 files pass (Zero Mojibake)
