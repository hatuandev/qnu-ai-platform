# MEMORY SNAPSHOT — Phiên Làm Việc #168
# Ngày: 2026-09-20 | Nội dung: Loại bỏ chuyển hướng /workflows/* & Giữ nguyên không gian URL /assistants/:id/workflow

## Trạng Thái Tại Thời Điểm Snapshot

- Thời gian: 2026-09-20 23:34 (UTC+7)
- Phiên số: #168
- Giai đoạn vừa hoàn thành:
  - Khắc phục triệt để vấn đề chuyển hướng làm mất ngữ cảnh Trợ lý AI:
    - Loại bỏ hoàn toàn hook tự động chuyển hướng sang `http://localhost:3001/workflows/...` và tham số query rườm rà `?returnTo=...`.
    - Chuẩn hóa toàn bộ các điểm truy cập DAG Studio trong Trợ lý AI (Header button, Header dropdown, Tools section button, Workspace nav tab) đều điều hướng chuẩn về URL: `/assistants/:code/workflow`.
    - Khi ở subView `workflow`, `AssistantDetailPage` trả về trực tiếp `DAGCanvasPage` toàn màn hình (`h-[calc(100vh-4rem)]`), không bị lồng ghép form hay tabs gây chật chội.
    - Duy trì 100% ngữ cảnh ứng dụng: Sidebar vẫn sáng ở "Trợ lý AI", Breadcrumb hiển thị "Xây Dựng AI > Trợ Lý AI", có nút quay lại `[Quay lại Trợ lý]` để trở về trang cấu hình `/assistants/:id`.
- Frontend Quality:
  - `npm run typecheck`: Pass (0 lỗi, `tsc --noEmit`).
  - `npm run lint`: Pass (0 lỗi, Biome 167 files sạch).
  - `npm run build`: Pass (Vite build production thành công trong 8.73s).
- Backend Quality:
  - `uv run ruff check .`: Pass (All checks passed).

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
|---|---|---|
| `frontend/src/pages/assistant-detail-page.tsx` | Sửa | Xóa bỏ hook `useEffect` chuyển hướng sang `/workflows/...`; render trực tiếp `DAGCanvasPage` toàn màn hình khi `subView === "workflow"`; bổ sung `backPath` và `backLabel`. |
| `frontend/src/components/assistants/assistant-header.tsx` | Sửa | Đổi đường dẫn nút `[Sơ đồ DAG Studio]` và dropdown item sang `/assistants/:code/workflow`, xóa bỏ hoàn toàn `?returnTo=...`. |
| `frontend/src/components/assistants/sections/assistant-tools-section.tsx` | Sửa | Đổi đường dẫn nút mở DAG Studio sang `/assistants/:code/workflow`. |
| `frontend/src/components/assistants/assistant-workspace-nav.tsx` | Sửa | Thêm tab `workflow` ("Sơ đồ DAG Studio", icon `Network`) vào `WORKSPACE_TABS`. |
| `frontend/src/pages/dag-canvas-page.tsx` | Sửa | Hỗ trợ `backPath`, `backLabel`, `initialWorkflowId`; hiển thị nút quay lại rõ ràng trên thanh Studio. |

## Kết Quả Kiểm Thử
- `npm run typecheck`: Pass (0 lỗi)
- `npm run lint`: Pass (0 lỗi)
- `npm run build`: Pass (8.73s)
- `uv run ruff check .`: Pass (0 lỗi)
