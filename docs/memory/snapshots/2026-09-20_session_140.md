# MEMORY SNAPSHOT — Phiên Làm Việc #140
# Ngày: 2026-09-20 | Nội dung: Khắc Phục Lỗi AttributeError: 'tuple' object has no attribute 'x0' Khi Upload PDF

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-20 13:56 (UTC+7)
- Giai đoạn vừa hoàn thành: Khắc phục lỗi crash 500 khi upload tài liệu PDF có chứa bảng biểu trong PyMuPDF blocks parser
- Test Suite:
  * Backend: `uv run ruff check .` -> All checks passed! (0 lỗi)
  * Backend: `uv run --extra dev pytest tests/test_knowledge.py -v` -> 31/31 passed (100%)
  * Frontend: `npm run lint` -> 165 files passed (0 lỗi, 0 cảnh báo)
  * Frontend: `npm run typecheck` -> 0 lỗi
  * Frontend: `npm run build` -> `tsc -b && vite build` built in 9.07s (dist generated successfully)

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/parsers/blocks.py` | MODIFY | Bổ sung hàm `_get_bbox_coords` an toàn đa hình xử lý cả `tuple`, `list`, `fitz.Rect` và `dict`; chuẩn hóa `table_bboxes` dạng `tuple[float, float, float, float]` |

## Kết Quả Kiểm Thử
- uv run ruff check .: All checks passed! 0 lỗi.
- uv run --extra dev pytest tests/test_knowledge.py -v: 31 passed, 1 warning in 47.22s.
