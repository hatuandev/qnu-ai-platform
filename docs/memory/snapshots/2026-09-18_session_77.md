# MEMORY SNAPSHOT — Phiên Làm Việc #77
# Ngày: 2026-09-18 01:25 (UTC+7) | Nội dung: Xử lý triệt để lỗi chuẩn hóa PDF scan & xóa bỏ hoàn toàn text giả lập

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-18 01:25 (UTC+7)
- Giai đoạn vừa hoàn thành: Xử lý triệt để lỗi PDF scan (QĐ 4740 BGDĐT), xóa sạch placeholder text giả lập, đồng bộ tracking `page_number` trong Chunker, và kích hoạt auto-rescue trong Studio View.
- Test Suite:
  * Backend Pytest: 44/44 passed (100%)
  * Backend Ruff: 0 error
  * Zero Mojibake Audit: 225/225 files sạch (100%)
  * Frontend Biome: 0 error (91 files)
  * Frontend Typecheck: 0 error (`tsc --noEmit`)
  * Frontend Build: Thành công trong 12.04s

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/ocr/layout_detector.py` | Chỉnh sửa | Triệt tiêu hoàn toàn text giả lập (`"Đoạn văn bản quy định"`, `"Tiêu đề đầu trang"`, `"Bảng biểu số liệu"`, `"Con dấu & Chữ ký xác thực"`) khỏi `text` và `content_snippet` của box. Gộp nhánh `title` theo ruff SIM114. |
| `backend/app/modules/knowledge/chunker.py` | Chỉnh sửa | Bổ sung `_RE_PAGE_MARKER` và tracking `page_number` trong `ClauseBasedChunker` và `SemanticChunker`. Gán đúng `page_number` cho từng chunk (1 đến 9), triệt tiêu lỗi `is_clumped` giả mạo. |
| `backend/app/modules/ocr/adapters/mistral_adapter.py` | Chỉnh sửa | Truyền markdown THẬT của từng trang từ Mistral OCR vào `SmartLayoutDetector` thay vì chuỗi rỗng `page.get_text()`. |
| `backend/app/modules/knowledge/service.py` | Chỉnh sửa | Trích xuất và lưu `page_markdowns`, lọc placeholder rác trong `_synthesize_page_markdown_from_blocks`, kích hoạt auto-rescue tự động khi doc rỗng hoặc khi bấm `[Quét lại]`. |
| `backend/tests/test_knowledge.py` | Chỉnh sửa | Bổ sung 2 test case: tracking `page_number` và từ chối placeholder rác. |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: All checks passed!
- `uv run --extra dev pytest tests/test_ocr.py tests/test_knowledge.py tests/test_smart_layout.py`: 44 passed in 46.65s (100%)
- `uv run python scripts/check_mojibake.py`: 225/225 files sạch (100%)
- `npm run lint`: Checked 91 files in 149ms. No fixes applied.
- `npm run typecheck`: 0 error.
- `npm run build`: built in 12.04s.
