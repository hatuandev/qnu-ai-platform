# MEMORY SNAPSHOT — Phiên Làm Việc #143
# Ngày: 2026-09-20 | Nội dung: Loại bỏ phân loại list tràn lan & Chuẩn hóa nhãn Mistral OCR không đè chữ

## Trạng Thái Tại Thời Điểm Snapshot
- Thời gian: 2026-09-20 14:21 UTC+7
- Mục tiêu hoàn thành:
  1. Loại bỏ phân loại `list` (xanh ngọc) tràn lan, đưa toàn bộ về `text` (tím nhẹ) như ban đầu chuẩn Mistral Document AI OCR.
  2. Siết chặt `header`: chỉ nhận diện ở đỉnh trang ($\le 16\%$) và bắt đầu bằng quốc hiệu/tên trường/số hiệu; không còn gán nhầm các đoạn ở giữa trang thành header.
  3. Siết chặt `title`: chỉ nhận diện số La Mã lớn (`I.`, `II.`), loại văn bản (`THÔNG BÁO`, `QUYẾT ĐỊNH`), hoặc dòng in hoa ngắn.
  4. Đặt Pill Badge ra ngoài viền hộp `-top-[13px] left-[-1px]` với kích thước siêu nhỏ `text-[8px] h-[13px]`, bảo đảm không bao giờ đè lên chữ bên trong hộp.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/knowledge/parsers/blocks.py` | Sửa đổi | Xóa bỏ `list`, siết chặt `header` và `title`, thỏa mãn Ruff SIM102 |
| `frontend/src/pages/scan-studio-page.tsx` | Sửa đổi | Xóa bỏ `list` trong `classifyStudioRegion`, chuẩn hóa regex |
| `frontend/src/components/knowledge/ocr/ocr-canvas.tsx` | Sửa đổi | Badge chuyển sang `-top-[13px] left-[-1px]` bên ngoài viền hộp |
| `docs/nhat_ky/2026-09-20_phien_143_loai_bo_list_va_chuan_hoa_badge_mistral.md` | Tạo mới | Ghi chép chi tiết phiên 143 |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: Passed (0 errors)
- `npm run lint`: Checked 165 files, 0 errors
- `npm run typecheck`: 0 errors
- `npm run build`: vite build thành công trong 6.56s
