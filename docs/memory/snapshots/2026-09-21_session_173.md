# MEMORY SNAPSHOT — Phiên Làm Việc #173
# Ngày: 2026-09-21 | Nội dung: Chuẩn Hóa Node DAG Query Rewrite Dùng Chung Đa Quy Trình & Visual Prompt Inspector

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-21 14:22 (UTC+7)
- **Giai đoạn vừa hoàn thành**:
  * Loại bỏ hoàn toàn bảng ánh xạ cố định 5 mô đun (`DOMAIN_CONTEXT_MAP`) trong `query_rewrite_node.py` để node `query.rewrite` trở thành node dùng chung (generic building block) cho mọi quy trình (tuyển sinh, đào tạo, ký túc xá, thư viện, khảo thí, công tác sinh viên,...).
  * Cho phép mỗi khi thêm node vào bất kỳ quy trình nào, người thiết kế có thể cấu hình `instruction` / `prompt` riêng biệt cho node đó theo đúng nghiệp vụ quy trình muốn tạo.
  * Tích hợp giao diện trực quan tại `PropertyInspector` (DAG Visual Studio): Textarea soạn `instruction` / `prompt` kèm toggles cho Fast Rules (0ms) và Tầng LLM (~150ms).
  * Cập nhật schema manifest `configs/nodes/query.rewrite.v1alpha1.json` và cấu hình workflow `configs/workflows/admissions-assistant.v1alpha1.json` trong CSDL PostgreSQL.
- **Trạng thái Test Suite**:
  * Backend Ruff: 0 lỗi
  * Backend Pytest: 100% passed (test_query_rewrite_node.py: 7/7 passed; test_workflows.py: 24/24 passed)
  * Frontend Biome: 168 files checked, 0 lỗi
  * Frontend TypeScript: 0 lỗi
  * Frontend Vite Build: Thành công trong 8.15s
  * End-to-end Chat Test: 100% pass với câu hỏi gõ nhầm 'ngày cntt' được chuẩn hóa và trả về đầy đủ học phí kèm 4 trích dẫn PDF.

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :--- | :--- |
| `backend/app/modules/workflows/nodes/query_rewrite_node.py` | Chỉnh sửa | Xóa bỏ `DOMAIN_CONTEXT_MAP` cố định 5 mô đun; bổ sung hàm `get_node_instruction` ưu tiên `instruction`/`prompt` cấp node; hỗ trợ placeholder `{query}` |
| `configs/nodes/query.rewrite.v1alpha1.json` | Chỉnh sửa | Bổ sung thuộc tính `instruction` và `prompt` vào `config_schema` |
| `configs/workflows/admissions-assistant.v1alpha1.json` | Chỉnh sửa | Bổ sung `instruction` chuẩn hóa tuyển sinh trực tiếp vào cấu hình node `query_rewrite` |
| `frontend/src/components/admin/property-inspector.tsx` | Chỉnh sửa | Tích hợp form cấu hình trực quan cho node `query.rewrite`: Textarea Prompt/Instruction, Switch Fast Rules, Switch LLM |
| `backend/tests/test_query_rewrite_node.py` | Chỉnh sửa | Cập nhật bộ unit test kiểm thử phân giải `instruction` linh hoạt và build prompt động |

## Kết Quả Kiểm Thử
- `uv run ruff check .`: 0 lỗi linter
- `uv run --extra dev pytest tests/test_query_rewrite_node.py tests/test_workflows.py -v`: **31/31 passed (100%)**
- `npm run lint`: Biome check 168 files, **0 lỗi**
- `npm run typecheck`: TypeScript tsc --noEmit, **0 lỗi**
- `npm run build`: Vite build đóng gói bundle thành công trong **8.15s**
- End-to-end Chat Verification: Chuẩn hóa thành công, trả về đủ học phí 83-97 triệu, 112,3 triệu và 1,5x cùng 4 trích dẫn nguồn PDF hợp lệ.
