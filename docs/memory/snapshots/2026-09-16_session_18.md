# MEMORY SNAPSHOT — Phiên Làm Việc #18
# Ngày: 2026-09-16 | Nội dung: Triển Khai Màn Hình Chi Tiết Provider Riêng Biệt & Toggle Switch Bật/Tắt

## Trạng Thái Tại Thời Điểm Snapshot
- **Thời gian**: 2026-09-16 11:30 (UTC+7)
- **Giai đoạn vừa hoàn thành**: Tách biệt Màn hình Chi tiết Provider độc lập khi click vào Card ở màn hình `/models` để tạo không gian rộng rãi, thoáng đãng, trải nghiệm UX tốt nhất (theo chuẩn `qnu-ai-core`). Tích hợp Toggle Switch bật/tắt Provider trực tiếp ở từng card danh sách và ở Hero Header màn hình chi tiết.
- **Test Suite**:
  - Pytest Backend: 70/70 passed
  - Playwright E2E: 15/15 passed
  - Ruff: 0 errors
  - Biome: 0 errors
  - TypeScript: 0 errors
  - Vite Build: Thành công

## Tóm Tắt Thay Đổi Trong Phiên Này
| Tệp | Hành Động | Mô Tả |
| :--- | :---: | :--- |
| `frontend/src/App.tsx` | MODIFY | Cập nhật điều kiện routing cho `/models` và `/models/:id`, truyền `currentPath` và `onNavigate` vào `ModelOpsPage`. |
| `frontend/src/pages/modelops-page.tsx` | MODIFY | Refactor toàn diện sang 2 chế độ hiển thị: Overview View với Toggle Switch trên từng Card (có `e.stopPropagation()`), click vào Card chuyển sang Detail View; Detail View toàn màn hình rộng rãi với Hero Header Card (Toggle Switch, Ping test, Edit, Delete), Key Pool toàn chiều rộng (bảng keys, progress token quota, status badges, test key, simulate 429 rotation), và Models Manager (thêm/xóa tag model, gợi ý 1-click từ Presets). |
| `frontend/src/services/api-client.ts` | MODIFY | Bổ sung `suggested_models?: string[]` vào `ProviderPreset` và thêm hàm `toggleProviderKey()`. |
| `frontend/tests/e2e/04_modelops_key_pool.spec.ts` | MODIFY | Nâng cấp test cases kiểm tra Màn hình Chi tiết Provider, Toggle Switch trên Card & Header, và nút quay lại danh sách. |

## Kết Quả Kiểm Thử
- `npm run lint`: Checked 58 files — **0 errors**
- `npm run typecheck`: tsc --noEmit — **0 errors**
- `npm run build`: vite build ✓ built in 8.95s — **Thành công**
- `uv run ruff check .`: All checks passed — **0 errors**
- `uv run --extra dev pytest -v`: **70/70 passed** (15.01s)
- `npx playwright test`: **15/15 passed** (35.2s)
