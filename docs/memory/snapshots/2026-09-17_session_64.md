# MEMORY SNAPSHOT — Phiên Làm Việc #64
# Ngày: 2026-09-17 | Nội dung: Tính năng thiết lập Model mặc định Hệ thống (Embedding, Reranker, OCR) cho Kho Tri Thức & RAG

## Trạng Thái Tại Thời Điểm Snapshot

- Thời gian: 2026-09-17 21:52 (UTC+7)
- Phiên số: #64
- Giai đoạn vừa hoàn thành: Tính năng chọn và thiết lập mô hình mặc định hệ thống cho Embedding (Cloudflare BGE-M3 vs Local SentenceTransformers), Reranker (Cloudflare BGE Reranker vs Local RRF Fallback), và OCR (Mistral OCR vs Local Docling/PyMuPDF); tự động phản ánh và đồng bộ trên Kho Tri Thức, Vector Ingestion và RAG Retrieval.
- Database: PostgreSQL lưu trữ cấu hình `system_model_defaults` trong bảng `model_provider_configs`, tự động đồng bộ vào runtime `settings` khi startup.
- Backend Test Suite: 135/135 tests passed (100% pass), Ruff check 0 lỗi.
- Frontend Build: `npm run lint` 0 lỗi (Biome), `npm run typecheck` 0 lỗi, `npm run build` đóng gói bundle thành công.

## Tóm Tắt Thay Đổi Trong Phiên Này

| Tệp | Hành Động | Mô Tả |
|---|---|---|
| `backend/app/modules/modelops/schemas.py` | Sửa | Khai báo schema `ModelOption`, `SystemModelDefaults`, `SystemModelDefaultsUpdate`, `SystemModelDefaultsResponse`, `SetDefaultModelRequest`. |
| `backend/app/modules/modelops/service.py` | Sửa | Cung cấp `get_system_model_defaults`, `update_system_model_defaults`, `set_provider_model_as_default` với cơ chế dynamic provider/model discovery. |
| `backend/app/modules/modelops/router.py` | Sửa | Bổ sung 3 REST endpoints: `GET /modelops/defaults`, `PUT /modelops/defaults`, `POST /modelops/providers/{id}/set-default`. |
| `backend/app/main.py` | Sửa | Nạp cấu hình `system_model_defaults` từ DB vào `settings.EMBEDDING_PROVIDER`, `settings.RERANKER_PROVIDER`, `settings.EMBEDDING_MODEL` trong lifespan startup. |
| `backend/tests/test_modelops.py` | Sửa | Bổ sung unit test `test_system_model_defaults_api` kiểm thử toàn diện vòng đời lấy, cập nhật và gán default model. |
| `frontend/src/services/api-client.ts` | Sửa | Bổ sung interfaces `ModelOption`, `SystemModelDefaults`, `SystemModelDefaultsResponse` và các methods `getSystemModelDefaults()`, `updateSystemModelDefaults()`, `setProviderModelAsDefault()`. |
| `frontend/src/pages/modelops-page.tsx` | Sửa | Render control card "Mô Hình Mặc Định Hệ Thống (Active System Defaults)" với 3 dropdowns tương tác thời gian thực; thêm badge "★ Default" và nút gán nhanh "Đặt Default" trên từng tag model. |
| `frontend/src/pages/knowledge-page.tsx` | Sửa | Tích hợp hiển thị badge model mặc định hệ thống trên thanh Header và trên từng Card kho tri thức (phân biệt icon `Zap` cho Cloudflare Edge vs `Cpu` cho Local). |
| `frontend/src/pages/collection-detail-page.tsx` | Sửa | Hiển thị chính xác tên mô hình embedding mặc định hệ thống thay cho chuỗi gán cứng. |

## Kết Quả Kiểm Thử

- `npm run lint`: 0 lỗi (Biome check 83 files).
- `npm run typecheck`: 0 lỗi (tsc --noEmit).
- `npm run build`: Thành công (Vite bundle production).
- `uv run ruff check .`: All checks passed (0 lỗi).
- `uv run --extra dev pytest -v tests/test_modelops.py`: 13/13 passed.
- `uv run --extra dev pytest -v tests/test_rag.py tests/test_knowledge.py`: 34/34 passed.
